# План

Обновлён: 15.09.2026

Здесь только то, что предстоит. Что уже сделано — в Журнале.

## Логика порядка

Порядок выбран человеком 15.09.2026 из трёх вариантов. Четыре соображения, три из них проверены на «Дневниках» и «Делу Время».

**Синхронизация — раньше новых сущностей.** Прикручивать её к учёту и блюдам дешевле, чем к учёту, блюдам, нормам и шаблонам. Она задевает раскладку, надгробия и слияние одноимённых (Р-12).

**Отклик дня — в Этапе 1, а не отдельным этапом «аналитика».** Привычка держится только петлёй «записал → сразу увидел». Таблица умерла без неё.

**История таблицы — в Этапе 1.** Порядок блюд по частоте (Р-08) и нормы (Р-13) стоят на истории; с февралём–мартом в базе они проверяются на настоящих данных в день появления, как февраль в «Делу Время».

**Нормы — раньше шаблонов.** Вопрос приложения (Р-01) важнее, чем третий механизм удешевления записи: два первых — порядок по частоте и «как вчера» — уже в Этапе 1. Обратный порядок повторил бы таблицу: запись дешёвая, ответа нет.

---

## Хвосты закрытых этапов

Пока нет.

## Этап 0 — Каркас

Вопрос этапа: стоит ли на телефоне пустая «Трапеза», которая работает без сети и обновляется сама?

1. **Репозиторий.** «Где живут код и сайт?» Settings → Pages → Source → **GitHub Actions**, иначе последний шаг деплоя падает с «Not Found».
2. **Ядро байт в байт.** «Что взято из «Делу Время» и с какого коммита?» `git archive d86f0aa` — файлы строки «0» таблицы Р-16: `core/` целиком, `sw.ts`, «Копия» и «О приложении» настроек, «Что нового», отчёт об ошибке, прогон, скрипт иконок, конфиги — отдельным коммитом как есть (Р-06, Р-16).
3. **Правки по делу проекта.** «Что разошлось с «Делу Время»?» Следующим коммитом: модель «Трапезы» дословно из Архитектуры, `SCHEMA_VERSION = 1`, пустые `migrations`; `SYNCED_STORES` и `V1_STORES` — пять хранилищ; `PLACES` раскладки; имя базы `trapeza`; `base` `/Trapeza/`, манифест с ярлыком «Записать»; «Сегодня» — своё, пустое. Синхронизация к экранам не подключается — только скопирована с тестами.
4. **Деплой.** «Доезжает ли сборка до сайта?» `deploy.yml`: test → build → upload-pages-artifact → deploy-pages.
5. **Копия файлом.** «Можно ли унести данные с первого дня?» Экспорт и восстановление из «Настроек».
6. **Прогон в браузере.** «Нет ли ошибок в консоли, открывается ли без сети?» `npm run smoke`, до первых настоящих экранов.

**Готово когда:** приложение ставится иконкой на телефон, открывается без сети, новая сборка доехала до телефона сама — видно по дате сборки в «Настройках»; тесты и прогон — код выхода 0.

## Этап 1 — Учёт

Вопрос этапа: записывается ли обычный день за минуту, и сходится ли история с таблицей?

1. **Блюда и категории.** «Как завести блюдо и поправить калорийность?» Экран «Блюда»; id из названия, слияние одноимённых, удаление с переносом (Р-12).
2. **Импорт.** «Как влить стартовые блюда?» Формат `trapeza-import`, промпт для ИИ из описаний полей, сводка до записи (Р-03). Стартовый список — из комментариев таблицы.
3. **История таблицы.** «Сходятся ли числа?» Разовый скрипт: xlsx → `trapeza-import` в `/seed/`, отчёт расхождений числа и цвета (Р-09). Тест настоящих данных читает `seed/`, без файла пропускается.
4. **«Сегодня».** «Как записать приём?» Четыре приёма, приём по часам (`mealHours`), порции и граммы, время и заметка по желанию, любой день задним числом (Р-11, Р-14).
5. **Порядок по частоте и «как вчера».** «Сколько тапов стоит обычный приём?» (Р-08, пп. 1–2).
6. **Итог дня.** «Что изменилось после записи?» По группам и категориям; калории с основанием (Р-01).

**Готово когда:** февраль в приложении — 271 порция, март — 162, как суммы таблицы; стартовые блюда стоят в справочнике; обычный день записан на телефоне меньше чем за минуту.

## Этап 2 — Синхронизация

Вопрос этапа: видна ли запись с телефона на компьютере?

1. **Подключение прохода.** «Уезжает ли запись сама?» Автозапуск, экраны перечитывают данные по `db.onChange`.
2. **Настройки.** «Как подключить репозиторий данных?» Репозиторий, токен, «Проверить доступ», срок токена.
3. **Живой запрос.** «Работает ли с настоящим GitHub?» Первый файл в пустой `TrapezaData` — через Contents API; тест проверял бы догадку, а не GitHub.
4. **Слияние одноимённых после прихода данных.** «Не завелось ли два «Борща»?» (Р-12).

**Готово когда:** запись с телефона появляется на компьютере; режим полёта ничего не ломает — очередь уходит, когда сеть вернулась.

**После этапа** — решение о выносе общего ядра по трём диффам (Р-06).

## Этап 3 — Нормы и неделя

Вопрос этапа: отвечает ли приложение на свой вопрос?

1. **Нормы.** «Как задать «сладкое — не больше 4 дней»?» `Norm` над набором категорий, `since` необязательно (Р-13).
2. **Отклик после записи.** «Что задела эта запись?» Строка нормы на «Сегодня» сразу после записи.
3. **Экран «Неделя».** «Чего было много и чего мало?» Нормы с историей, состав по группам, приёмы по дням, калории в среднем с основанием; любая неделя.
4. **Тон отклика.** «Красить ли превышение?» Открытый вопрос в `05`, решается до кода этапа.

**Готово когда:** норма, заведённая в день этапа, показывает историю февраля–марта; после записи сладкого строка нормы меняется сразу.

## Этап 4 — Повтор приёма

Вопрос этапа: стоит ли обычный день четыре тапа?

1. **Свои шаблоны.** «Как сохранить привычный завтрак?» «Сохранить как шаблон» из записанного приёма, применение без дублей (Р-08, п. 3).
2. **«Как обычно?» при открытии.** «Что с незаписанными приёмами?» Сегодня и вчера; шаблон приёма, иначе «как вчера».
3. **Напоминание.** «Что, если забыл вовсе?» Окно раз в сутки, «сегодня ничего не записано», журнал пробуждений — механика «Делу Время» (их Р-14, Р-24). Что считать незаполненным днём — решается в этапе.
4. **Ярлык «Записать».** «Как записать, не открывая экран?» Долгий тап по иконке.

**Готово когда:** обычный день — завтрак, обед, ужин «как обычно» и одно отклонение — четыре тапа. Приход напоминания проверяется днями, хвостом.

## Этап 5 — Лента, выгрузка, справка

Вопрос этапа: находится ли любая запись, и понятно ли приложение без меня?

1. **Лента `#/feed`.** «Что я ел в тот день?» Строка на день, поиск.
2. **Markdown.** «Как унести историю текстом?» Разделы и период на выбор.
3. **Справка `#/help`.** «Как работает повтор и нормы?» Числа — только из констант, тест-сторож цифр.
4. **«Что нового», отчёт об ошибке, приветствие.** «Что изменилось после обновления?»

**Готово когда:** запись любого дня находится поиском ленты; справка объясняет три механизма повтора; «Что нового» показывает правку после обновления.

## Этап 6 — Рецепты

Вопрос этапа: сколько калорий в моём борще? Не раньше, чем учёт проживёт месяц (Р-07).

1. **Состав и выход.** «Как посчитать калорийность домашнего блюда?» Поля `recipe`, расчёт с основанием.
2. **Проверка цепочки.** «Не ссылается ли блюдо само на себя?»

**Готово когда:** калорийность своего борща посчитана из состава и названа с основанием.

## Этап 7 — Отдать другу

Вопрос этапа: работает ли это у постороннего человека? **Отложен до отдачи вместе с семейством приложений** — как в «Делу Время» (их Р-67): личное из кода и промптов, гайд установки, README кода, проверка миграции на копии, iPhone.

---

## Отложено

| Что | Почему не сейчас | Когда вернуться |
|---|---|---|
| Open Food Facts по штрихкоду | упакованного около 10%, нормы видят его по категории (Р-03) | если руками по этикетке окажется утомительно: один запрос при заведении блюда |
| Обёртка в APK: точные напоминания, виджет | довод не подтверждён жизнью (Р-04) | через четыре недели жизни, если приёмы теряются от забывания в моменте |
| Итоги месяца и года | неделя — ритм вопроса (Р-01) | когда неделя проживёт месяц |
| Цель по калориям и весу | калорийность — оценка (Р-01) | новым решением, вместе с рецептами |
| Время само по умолчанию | врёт на записях задним числом (Р-14) | только для текущего приёма сегодняшнего дня |
| Интеграции с «Дневниками» и «Делу Время» | метаприложение семьи — после всех приложений (Р-05, Р-06) | когда семейство готово |
| Вынос общего ядра | нужен третий дифф (Р-06) | после Этапа 2 |
| iPhone | не проверялся; у установленного своё хранилище | к отдаче другу |

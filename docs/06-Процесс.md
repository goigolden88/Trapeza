# Процесс

Обновлён: 18.09.2026

Как ведётся работа над проектом. Не про то, что строим, а про то, как.

## Где что живёт

| Что | Где |
|---|---|
| Код приложения | `github.com/goigolden88/Trapeza`, публичный; сайт — `goigolden88.github.io/Trapeza/`. Локально — `C:\Ф\Prognananda\Claude Projs\Еда\Trapeza` |
| Документы проекта | `docs/` в том же репозитории |
| Инструкции для Claude Code | `CLAUDE.md` в корне репозитория |
| Данные пользователя | IndexedDB на устройстве; с Этапа 2 — приватный репозиторий `TrapezaData` |
| Исходная таблица | `Еда\Исходники\Учет Еды 2026.xlsx` — вне репозитория, личные данные |
| Общее ядро и договор семьи | `github.com/goigolden88/FamilyCore`, публичный — **эталон ядра** с 18.09.2026 (Р-46, Р-49, Р-50). Здесь — подмодулем в `src/shared`; свой клон для сессий ядра — `C:\Ф\Prognananda\Claude Projs\FamilyCore` |
| Прежний эталон | «Делу Время», коммит `d86f0aa` — точка рождения ядра, не источник истины (Р-49). `github.com/goigolden88/DeluVremya`, локально `ТаймМенеджмент\DeluVremya` — **только для чтения** |
| Первое приложение семьи | «Дневники», `github.com/goigolden88/Dnevniki` и `Дневники\Dnevniki` — **только для чтения** |

Документы лежат вместе с кодом: `CLAUDE.md` подхватывается автоматически, а документы версионируются заодно с кодом. Копия в проекте claude.ai — снимок; правится только репозиторий.

### Сессии Claude Code

Запускать из корня репозитория `Еда\Trapeza`: история бесед привязана к рабочей папке. Сессия планирования шла из папки `Еда` — до того, как репозиторий появился; дальше — только из корня. Между этапами — `/clear`, `/compact` — для середины этапа. Перед `/clear`: дерево чистое, всё запушено, и по одному Журналу понятно, где мы и что дальше.

### «Делу Время» и «Дневники» — только читать

Из их репозиториев ничего не правится и не коммитится, даже найденная ошибка: о ней говорится человеку.

**Единственное исключение — перевод на ядро** (Р-49). Он делается **сессией из корня того репозитория**, по его документам, со своим решением Р-NN в его `05`. Из «Трапезы» чужие репозитории не правятся никогда — ни ради ядра, ни ради общей беды.

**Как читать «Р-NN»** (Р-53, уточняет Р-43). Голый номер в своих файлах — решение «Трапезы». Чужой пишется с хозяином: «Р-78 „Делу Время“», «их Р-63». В `src/shared` у каждой ссылки хозяин — «Р-47 „Трапезы“», «Р-07 „Дневников“», — а решения ядра — «Я-NN» (Я-01 «FamilyCore»). Номера «Делу Время» и «Дневников» пересекаются: хозяина определяет тема, а не номер, и сверяется он по их `05` через `git show`.

### Подмодуль ядра

Действует с 18.09.2026: «Трапеза» переведена на `FamilyCore` коммитом-переездом `78eefc9` (Р-46, Р-49).

- **Клон** — только `git clone --recurse-submodules`; уже склонированное без него — `git submodule update --init`. Иначе `src/shared` пустая и сборка падает непонятной ошибкой
- **`src/shared` отсюда не правится.** Ядру чего-то не хватает, в нём ошибка или `db` и `sync` не подходят под его типы без `as` — сказать человеку, что и где, и остановиться. Правка — сессией из корня `FamilyCore` по её передаче «сопровождение», решением Я-NN, с тестом на «Полке» и зелёным CI ядра
- **Обновление ядра здесь** — отдельная сессия или шаг: `git submodule update --remote src/shared` (или `git -C src/shared checkout <коммит>`), затем `npm test`, `npm run build`, `npm run smoke` по коду выхода и **коммит указателя**. Забытый указатель — правки нет ни у второго устройства, ни в CI. Видимое человеку изменение ядра — строка в `src/changes.ts` в том же коммите
- **Перед пушем** — `git status` чистый **и в подмодуле тоже**: `git -C src/shared status`
- **В `src/shared` не запускать `npm install`** — заведётся `src/shared/node_modules` со второй копией React (Я-02 «FamilyCore»)
- **Тесты ядра здесь не гоняются, типы — проверяются** (Р-52): `npm test` — только свои; `tsc` в `npm run build` проверяет `src/shared` целиком. Число тестов «Трапезы» с 18.09.2026 — 203, а не 491: 297 ушли с ядром
- **Откат** перевода — `git revert 78eefc9`; откат обновления ядра — указатель на прежний коммит, одна строка в диффе. Точка невозврата — первая миграция схемы поверх ядра (Р-49)
- Поломка в ядре чинится один раз, в `FamilyCore`; «Делу Время» и «Дневники», пока не переведены, держат свои копии — про каждую говорится человеку

## Разделение работы

**Claude Code — и «что строить», и «как».** Продуктовые развилки и пересмотры архитектуры — тоже здесь: он видит настоящие файлы и проверяет факты. Режим планирования — когда обсуждаем и ничего не трогаем.

**Чат (Claude Desktop, claude.ai) — когда компьютера нет под рукой**, для свободного разговора без кода и для картинок. Его итог приходит в репозиторий потоком ниже.

### Планирование в Claude Code

1. Сессия планирования из корня репозитория — заготовка в «Шаблонах»
2. Решения — в `05-Решения` в момент выбора; правки `01`, `02`, `03` — по ходу. В режиме планирования, когда писать можно только файл плана, решение пишется туда готовой записью в момент выбора и переносится в `05` дословно
3. Конец: Журнал, передача, коммит документов, дерево чистое
4. `/clear` — и код новой сессией по плану. Контекст планирования в неё не переезжает: память — документы

## Поток изменений из чата: чат → документы → код

1. Обсуждаем крупное изменение в чате
2. На выходе — готовые куски markdown для вставки: запись в `05-Решения`, правки `01-Проект` и `02-Архитектура`
3. Отдельным пунктом — что придётся переделать в уже написанном коде
4. Вставляем, коммитим
5. Следующая сессия Claude Code начинается со сверки

## Обратный поток: код → чат

То, что Claude Code в конце сессии называет «не описано в документах», возвращается в обсуждение, а не решается на месте.

## Правила ведения документов

- **Не менять задним числом.** Отмена решения — новая запись со ссылкой
- **Решения записываются в момент выбора**
- **Журнал обновляется в конце каждой сессии**
- **Перед пушем — `npm test`, `npm run build`, `npm run smoke`**, итог по коду выхода, а не по выводу
- **Прогон проверяется порчей кода**, когда меняется его сценарий: сломанная строка экрана обязана дать код 1
- **Прогон ходит в IndexedDB сам.** `scripts/smoke.mjs` открывает базу `trapeza` напрямую — снимает `seenChanges`, читает `folds`. Правило «к хранилищу только через `db` из `app/core.ts`» — про код приложения; прогон смотрит на собранный `dist` снаружи, из браузера (Р-43)
- **Порча — в отдельной рабочей копии, а не в основной папке:** `git worktree add ../Trapeza-spoil HEAD`, `node_modules` — переходом на основные (`mklink /J`), после — `git worktree remove --force ../Trapeza-spoil`. Так же, как в «Делу Время»
- **Видимое изменение — строка в `src/changes.ts`** в том же коммите
- **План содержит только предстоящее**
- **Цифра со сводного листа или экрана — вместе с формулой**, из которой она посчитана (Журнал, 15.09.2026, ошибка с «домашней едой»)

## Шаблоны

Начало сессии по плану:

```
Работаем по docs/03-План.md, следующий этап оттуда.
Прочитай CLAUDE.md и docs/ перед началом.
Сначала покажи план работ, код пиши после подтверждения.
```

Сессия планирования:

```
Планируем: <что — новый этап, модуль, развилка>. Прочитай CLAUDE.md
и docs/. Код не пишем — обсуждаем по одной развилке, с твоей
рекомендацией, и записываем решения в docs/ в момент выбора.
В конце — Журнал, передача в 06-Процесс, коммит документов.
```

Начало сессии после обновления документов в чате:

```
Я обновил docs/. Прочитай их и сверься с кодом.
Скажи, что теперь расходится и в каком порядке это править.
```

Конец сессии:

```
Дай текст для docs/04-Журнал.md: что сделано, в каком состоянии,
следующий шаг. И для docs/05-Решения.md, если по ходу что-то решили.
Отдельно — что осталось не описанным в документах.
```

### Передача в следующую сессию

Переписывается в конце каждого этапа. Этап 8 выполнен 18.09.2026: **Б1** — `FamilyCore` заведён и собирается без приложения (сессией из его корня), **Б2** — «Трапеза» переведена на ядро (этой сессией, Р-52…Р-54). Прежние тексты Б1 и Б2 — в истории git этого файла.

Дальше по очереди Р-49: **«Финансы»** — начинаются на ядре, сессией из своего корня, не отсюда; **А** — хвосты и починка «Трапезы», обычная рабочая сессия. Если «Финансы» найдут что-то в ядре, это сессия сопровождения `FamilyCore`, а сюда приходит только обновление указателя.

#### А. Хвосты закрытых этапов и починка

```
Проект «Трапеза» — учёт еды, третье приложение семьи. Работать в корне
репозитория `C:\Ф\Prognananda\Claude Projs\Еда\Trapeza`.

Прочитай `CLAUDE.md` и все файлы `docs/` перед началом.

Этапы 0–6 и 8 закрыты. С 18.09.2026 «Трапеза» — на общем ядре `FamilyCore`,
подмодулем в `src/shared` (Р-45…Р-54): своё — `src/app/` (конфиг, модель,
сборка ядра), `modules/food`, `screens/`, `registry.ts`, `notify.ts`.
`src/shared` отсюда не правится. `gh` на компьютере нет; итог деплоя —
через api.github.com без токена.

**Работы по коду в Плане нет.** Сессия начинается не с кода, а с разговора:

1. Спроси про четыре хвоста (`docs/03-План.md`). Обычный день за минуту
   и в четыре тапа — шаблоном завтрака и «Как обычно?» он ещё не
   пользовался. Норм на телефоне нет — без них стоят три пункта: строка
   нормы после сладкого, история норм по всем неделям и «как вчера» без
   строки норм (Р-25). Напоминание само — журнал пробуждений в «Настройки» →
   «Напоминания». Рецепт у плиты — если перенос ответа ИИ в поле
   утомителен, в План идёт `share_target` (условие возврата в Р-41).
2. Что из этого делать — решает человек. Решение — в `05-Решения.md`
   в момент выбора, работа — в `03-План.md`, и только потом код: сначала
   план работ, код после подтверждения.

**Если человек принёс поломку** — эта же сессия её и чинит, но по порядку:

1. Сначала воспроизвести, а не поверить на слово. Уточни устройство, экран,
   что нажал и что ждал. Экран, который врёт, — это либо расчёт (падающий
   тест на чистой функции), либо разметка (проверка в `scripts/smoke.mjs`).
   Не воспроизводится — скажи прямо и предложи, что снять с телефона.
2. Найти причину и назвать её словами до правки. Поломка от прежнего
   решения — отменяется только новой записью в `05-Решения.md`.
3. **Причина в `src/shared`** — не чинить здесь. Сказать человеку, что
   и где, дать промпт для сессии сопровождения `FamilyCore` (шаблон —
   в её `06-Процесс`) и остановиться на этом месте. После правки ядра —
   `git submodule update --remote src/shared`, проверки, коммит указателя.
   Про копии «Делу Время» и «Дневников» — сказать словами.
4. Починка своего — вместе с проверкой, которая падала бы до неё. Проверка
   сначала, потом правка.
5. Видимое человеку — строкой в `src/changes.ts` в том же коммите.
6. Перед пушем — `npm test`, `npm run build`, `npm run smoke`, итог по коду
   выхода; менялся сценарий прогона — проверить порчей; `git status`
   чистый и в `src/shared`.

Этап 7 «Отдать другу» отложен до отдачи вместе с семейством приложений.

Ключевое, что нужно знать, когда дойдёт до кода:
— `db`, `sync`, `importing` — из `src/app/core.ts`; конфиг — `src/app/config.ts`
  (имя базы, хранилища, индексы, раскладка — не меняются без миграции);
  общие компоненты — `shared/ui/…`, они берут базу из `<CoreProvider>`
  в `app.tsx`; код приложения идёт в ядро пропсами (вкладки, `CHANGES`,
  строки ленты, разделы импорта, тема напоминания);
— ядро вызывается без приведений типов; не подходит без `as` — находка
  про ядро, не повод для `as`;
— `vitest` тесты ядра не гоняет, `tsc` проверяет (Р-52); в `src/shared` —
  не `npm install`;
— голый «Р-NN» в своих файлах — «Трапезы»; чужой — с хозяином (Р-53);
— рецепт (Р-39) — `recipe?: string` у блюда, текст по заготовке; калорийность
  из рецепта не считается. Id сворачивания `dishes:new:recipe`
  и `dishes:edit:recipe` — разные нарочно;
— поиск в выборе приёма есть всегда (Р-37); шаблон применяет архивное блюдо
  намеренно (Р-42), `share_target` не объявляется (Р-41), «Блюда» остаются
  вкладкой (Р-40);
— справка и приветствие — числа только из констант, сторож `help.test.ts`
  падает на любой цифре; прогон читает числа справки из исходников
  (`constant`), в том числе из `src/shared/notify.ts`;
— число вместе с основанием; отклик без цвета (Р-23); видимое изменение —
  строка в `src/changes.ts` в том же коммите (последний id — 22);
— личные данные — только в `seed/` под `.gitignore`: копия
  `trapeza-2026-09-17.json` для `npm run smoke -- --data`;
— правки файлов с кириллицей и замены с обратной косой чертой — инструментом
  правки или скриптом-файлом из папки сессии, записанным инструментом
  записи: heredoc Bash съедает `\` и ломается на кавычках; коммиты — Bash
  с heredoc;
— прогон: долгий — только в фоне; итог по коду выхода; вывод — в папку
  сессии; `npm run smoke` сам собирает `dist` — не править `src`, пока он
  идёт; два прогона разом делят порт отладки — только по очереди.
  Порча — в `git worktree`, `node_modules` переходом `mklink /J`
  из PowerShell, перед `worktree remove` переход снять `rmdir`.

Как работаем: сначала план работ, код после подтверждения. Решения —
в `docs/05-Решения.md` в момент выбора. Отступления от Архитектуры — вслух
до кода. В конце сессии — Журнал. Перед `/clear` дерево чистое и всё
запушено, в подмодуле тоже.
```

# Процесс

Обновлён: 18.09.2026

Как ведётся работа над проектом. Не про то, что строим, а про то, как.

## Где что живёт

| Что | Где |
|---|---|
| Код приложения | `github.com/goigolden88/Trapeza`, публичный; сайт — `goigolden88.github.io/Trapeza/`. Локально — `C:\Ф\Prognananda\Claude Projs\Еда\Trapeza` |
| Документы проекта | `docs/` в том же репозитории |
| Инструкции для Claude Code | `CLAUDE.md` в корне репозитория |
| Данные пользователя | IndexedDB на устройстве; с Этапа 2 — приватный репозиторий `TrapezaData` |
| Исходная таблица | `Еда\Исходники\Учет Еды 2026.xlsx` — вне репозитория, личные данные |
| Общее ядро и договор семьи | `github.com/goigolden88/FamilyCore`, публичный — **эталон ядра** с 18.09.2026 (Р-46, Р-49, Р-50). Подключается подмодулем в `src/shared` |
| Прежний эталон | «Делу Время», коммит `d86f0aa` — точка рождения ядра, не источник истины (Р-49). `github.com/goigolden88/DeluVremya`, локально `ТаймМенеджмент\DeluVremya` — **только для чтения** |
| Первое приложение семьи | «Дневники», `github.com/goigolden88/Dnevniki` и `Дневники\Dnevniki` — **только для чтения** |

Документы лежат вместе с кодом: `CLAUDE.md` подхватывается автоматически, а документы версионируются заодно с кодом. Копия в проекте claude.ai — снимок; правится только репозиторий.

### Сессии Claude Code

Запускать из корня репозитория `Еда\Trapeza`: история бесед привязана к рабочей папке. Сессия планирования шла из папки `Еда` — до того, как репозиторий появился; дальше — только из корня. Между этапами — `/clear`, `/compact` — для середины этапа. Перед `/clear`: дерево чистое, всё запушено, и по одному Журналу понятно, где мы и что дальше.

### «Делу Время» и «Дневники» — только читать

Из их репозиториев ничего не правится и не коммитится, даже найденная ошибка: о ней говорится человеку.

**Единственное исключение — перевод на ядро** (Р-49). Он делается **сессией из корня того репозитория**, по его документам, со своим решением Р-NN в его `05`. Из «Трапезы» чужие репозитории не правятся никогда — ни ради ядра, ни ради общей беды.

**Как читать «Р-NN» в файлах ядра.** В `core/`, `ui/`, `sw.ts`, `launch.ts`, `styles.css` ссылки перенесены дословно вместе с кодом и означают решения «Делу Время»: `core/dates.ts` говорит «неделя — с понедельника (Р-41)» — это их Р-41, а не наш. Номера уже пересеклись: свои Р-41…Р-44 появились 18.09.2026. Свои решения в этих файлах не заводятся; когда правка по делу проекта туда всё же идёт, она называет решение явно — «Р-NN „Трапезы“» (Р-43). После переезда в `FamilyCore` эти номера остаются без хозяина — как ядро нумерует своё, решается при его заведении (План, Этап 8).

### Подмодуль ядра

Действует с перевода «Трапезы» на `FamilyCore` (Р-46).

- Клон репозитория — только `git clone --recurse-submodules`; иначе `src/shared` пустая и сборка падает непонятной ошибкой
- Правка ядра — **два коммита в двух репозиториях**: сначала в `src/shared` с его тестами, потом указатель в приложении. Забытый указатель означает, что правки нет ни у второго устройства, ни в CI
- Перед пушем — `git status` обязан быть чистым **и в подмодуле тоже**
- Правка ядра ради одного приложения — повод остановиться: либо параметр в `AppConfig`, либо это не ядро
- Поломка в ядре чинится один раз, в `FamilyCore`; непереведённые приложения получают её отдельными сессиями из их корней

## Разделение работы

**Claude Code — и «что строить», и «как».** Продуктовые развилки и пересмотры архитектуры — тоже здесь: он видит настоящие файлы и проверяет факты. Режим планирования — когда обсуждаем и ничего не трогаем.

**Чат (Claude Desktop, claude.ai) — когда компьютера нет под рукой**, для свободного разговора без кода и для картинок. Его итог приходит в репозиторий потоком ниже.

### Планирование в Claude Code

1. Сессия планирования из корня репозитория — заготовка в «Шаблонах»
2. Решения — в `05-Решения` в момент выбора; правки `01`, `02`, `03` — по ходу. В режиме планирования, когда писать можно только файл плана, решение пишется туда готовой записью в момент выбора и переносится в `05` дословно
3. Конец: Журнал, передача, коммит документов, дерево чистое
4. `/clear` — и код новой сессией по плану. Контекст планирования в неё не переезжает: память — документы

## Поток изменений из чата: чат → документы → код

1. Обсуждаем крупное изменение в чате
2. На выходе — готовые куски markdown для вставки: запись в `05-Решения`, правки `01-Проект` и `02-Архитектура`
3. Отдельным пунктом — что придётся переделать в уже написанном коде
4. Вставляем, коммитим
5. Следующая сессия Claude Code начинается со сверки

## Обратный поток: код → чат

То, что Claude Code в конце сессии называет «не описано в документах», возвращается в обсуждение, а не решается на месте.

## Правила ведения документов

- **Не менять задним числом.** Отмена решения — новая запись со ссылкой
- **Решения записываются в момент выбора**
- **Журнал обновляется в конце каждой сессии**
- **Перед пушем — `npm test`, `npm run build`, `npm run smoke`**, итог по коду выхода, а не по выводу
- **Прогон проверяется порчей кода**, когда меняется его сценарий: сломанная строка экрана обязана дать код 1
- **Прогон ходит в IndexedDB сам.** `scripts/smoke.mjs` открывает базу `trapeza` напрямую — снимает `seenChanges`, читает `folds`. Правило «к хранилищу только через `core/db.ts`» — про код приложения; прогон смотрит на собранный `dist` снаружи, из браузера (Р-43)
- **Порча — в отдельной рабочей копии, а не в основной папке:** `git worktree add ../Trapeza-spoil HEAD`, `node_modules` — переходом на основные (`mklink /J`), после — `git worktree remove --force ../Trapeza-spoil`. Так же, как в «Делу Время»
- **Видимое изменение — строка в `src/changes.ts`** в том же коммите
- **План содержит только предстоящее**
- **Цифра со сводного листа или экрана — вместе с формулой**, из которой она посчитана (Журнал, 15.09.2026, ошибка с «домашней едой»)

## Шаблоны

Начало сессии по плану:

```
Работаем по docs/03-План.md, следующий этап оттуда.
Прочитай CLAUDE.md и docs/ перед началом.
Сначала покажи план работ, код пиши после подтверждения.
```

Сессия планирования:

```
Планируем: <что — новый этап, модуль, развилка>. Прочитай CLAUDE.md
и docs/. Код не пишем — обсуждаем по одной развилке, с твоей
рекомендацией, и записываем решения в docs/ в момент выбора.
В конце — Журнал, передача в 06-Процесс, коммит документов.
```

Начало сессии после обновления документов в чате:

```
Я обновил docs/. Прочитай их и сверься с кодом.
Скажи, что теперь расходится и в каком порядке это править.
```

Конец сессии:

```
Дай текст для docs/04-Журнал.md: что сделано, в каком состоянии,
следующий шаг. И для docs/05-Решения.md, если по ходу что-то решили.
Отдельно — что осталось не описанным в документах.
```

### Передача в следующую сессию

Переписывается в конце каждого этапа. Передач две: **А — хвосты и починка**
(обычная рабочая сессия, идёт по мере жизни) и **Б — Этап 8, вынос ядра**
(сессия кода, идёт один раз). С 18.09.2026 первой идёт Б: «Финансы»
начинаются сразу за ней, и заводить четвёртую копию ядра больше нельзя
(Р-45). Прежняя передача Б — сессия планирования выноса — выполнена
18.09.2026, её итог: Р-45…Р-51.

#### А. Хвосты закрытых этапов и починка

```
Проект «Трапеза» — учёт еды, третье приложение семьи. Работать в корне
репозитория `C:\Ф\Prognananda\Claude Projs\Еда\Trapeza`.

Прочитай `CLAUDE.md` и все файлы `docs/` перед началом.

Этапы 0–6 закрыты, последний — 18.09.2026: «Сегодня» с записью тапом,
«Блюда», импорт, история февраля–марта, синхронизация через `TrapezaData`,
«Неделя» с нормами, выбор блюда с поиском при любом числе блюд, шаблоны,
«Как обычно?», напоминание, лента ⌕, markdown за период, справка «?»,
приветствие и «Что нового», новое блюдо из поиска в приёме, рецепт блюда
текстом. Решения Р-01…Р-44 — см. `docs/05-Решения.md`. `gh` на компьютере
нет; итог деплоя — через api.github.com без токена.

**Работы по коду в Плане нет.** 18.09.2026 разобраны хвосты и «на
обсуждение»: пять пунктов закрыты решениями Р-40…Р-44 без кода, кроме одного
комментария в `styles.css`. Сессия снова начинается не с кода, а с разговора:

1. Спроси человека про четыре оставшихся хвоста (`docs/03-План.md`).
   Обычный день за минуту и в четыре тапа — шаблоном завтрака и «Как
   обычно?» он ещё не пользовался, обещал рассказать позже. Норм
   на телефоне по-прежнему нет — без них стоят три пункта разом: строка
   нормы после сладкого, история норм по всем неделям и «как вчера» без
   строки норм (Р-25). Напоминание само — не видел, заполнять не забывает,
   собирался нарочно пропустить день; журнал пробуждений в «Настройках» →
   «Напоминания». Рецепт у плиты — собирался проверить в настоящей готовке
   19.09; если перенос ответа ИИ в поле окажется утомительным, в План идёт
   `share_target` (условие возврата в Р-41).
2. «На обсуждение» осталось три: история норм по всем неделям и «как вчера»
   без строки норм — оба ждут первых норм; вынос ядра (Р-22) — когда
   «Трапеза» доделана.
3. Что из этого делать — решает человек. Решение — в `05-Решения.md`
   в момент выбора, работа — в `03-План.md`, и только потом код: сначала
   план работ, код после подтверждения.

**Если человек принёс поломку** — эта же сессия её и чинит, но по порядку,
а не сразу в код:

1. Сначала воспроизвести, а не поверить на слово. Уточни у человека
   устройство, экран, что нажал и что ждал увидеть. Экран, который врёт, —
   это либо расчёт (тогда падающий тест на чистой функции), либо разметка
   (тогда проверка в `scripts/smoke.mjs`). Не воспроизводится — скажи прямо
   и предложи, что снять с телефона: копию данных в `seed/`, снимок экрана,
   строку из «Настройки» → «О приложении».
2. Найти причину и назвать её словами до правки. Если поломка от прежнего
   решения — скажи, какого; чинить вопреки решению нельзя, можно только
   отменять его новой записью в `05-Решения.md`.
3. Починка — вместе с проверкой, которая падала бы до неё: тест на чистой
   функции, если считалось не то; проверка прогона, если врал экран.
   Проверка сначала, потом правка.
4. Видимое человеку — строкой в `src/changes.ts` в том же коммите. Мелкая
   починка невидимого (комментарий, тест) строки не получает.
5. Перед пушем — `npm test`, `npm run build`, `npm run smoke`, итог по коду
   выхода. Менялся сценарий прогона — проверить порчей: сломанная строка
   экрана обязана дать код 1.
6. Поломка в файлах ядра (`core/`, `ui/`, `sw.ts`, `launch.ts`, `styles.css`)
   есть и у «Дневников», и у «Делу Время»: правится только здесь, а про две
   другие копии говорится человеку словами (Р-06, Р-22, «Цена»).

Этап 7 «Отдать другу» отложен до отдачи вместе с семейством приложений
(как «Делу Время», их Р-67): личное из кода и промптов, гайд установки,
README кода, проверка миграции на копии, iPhone.

Ключевое, что нужно знать, когда дойдёт до кода:
— рецепт (Р-39) — `recipe?: string` у блюда, текст по заготовке; ссылок на
  ингредиенты нет, калорийность из рецепта не считается — её вводят руками,
  оценку даёт ИИ по промпту. Заготовка, промпт и `cleanRecipe` (пустая
  заготовка — не рецепт) — в `modules/food/recipe.ts`. Блок «Рецепт» —
  в форме блюда на «Блюдах», отдельно у «Нового блюда» и у правки: id
  сворачивания `dishes:new:recipe` и `dishes:edit:recipe`, одинаковые id
  писали бы в чужое поле (поймано прогоном);
— поиск в выборе приёма есть всегда (Р-37), порога по числу блюд нет;
  пустой справочник не прячет приёмы — подсказка стоит над ними;
— голый «Р-NN» в `core/`, `ui/`, `sw.ts`, `launch.ts`, `styles.css` — решение
  «Делу Время», а не «Трапезы»: номера пересеклись с Р-41 (Р-43). Свои
  решения в этих файлах не заводятся; шаблон применяет архивное блюдо
  намеренно (Р-42), `share_target` не объявляется (Р-41), «Блюда» остаются
  вкладкой насовсем (Р-40);
— справка («Итоги и калории», «Рецепт блюда») и приветствие — числа только
  из констант, сторож `help.test.ts` падает на любой цифре в исходнике;
— число вместе с основанием; отклик без цвета (Р-23); видимое изменение —
  строка в `src/changes.ts` в том же коммите (последний id — 22); маршруты
  и проверки прогона прибавляются вместе с экранами, в том числе
  в `dataScenario`;
— личные данные — только в `seed/` под `.gitignore`: копия
  `trapeza-2026-09-17.json` для `npm run smoke -- --data`, файл истории,
  скрипт; тест истории без файла пропускается;
— правки файлов с кириллицей — инструментом правки или скриптом-файлом
  из папки сессии: heredoc Bash с шаблонными строками JS съедает `\n`
  (снова в Этапе 6); коммиты — Bash с heredoc. Общий файл двух коммитов —
  версия в индекс через `git hash-object -w --path` и `git update-index
  --cacheinfo`;
— прогон: долгий — только в фоне; итог по коду выхода; вывод — в папку
  сессии; проверка текста — точной, не подстрокой; `npm run smoke` сам
  собирает `dist` — не собирать и не править `src`, пока он идёт; два
  прогона разом делят порт отладки — только по очереди, порча тоже; имена
  переменных сценария общие на файл — столкновение роняет весь прогон;
  в `run` помощников нет — `byText` и `set` только в `act`; `tapChip`
  разворачивает категории выбора; проверки формы блюда — внутри `.cat__body`;
  экран после сохранения читается, когда форма уже закрыта, иначе список ещё
  старый; числа справки прогон читает из исходников (`constant`);
  `repeatScenario` — последним, часть проверок зависит от часа машины.
  Порча — в `git worktree`, `node_modules` переходом `mklink /J`
  из PowerShell, перед `worktree remove` переход снять `rmdir`; одна порча —
  один прогон; несколько — скриптом-файлом по очереди.

Как работаем: сначала план работ, код после подтверждения. Решения —
в `docs/05-Решения.md` в момент выбора. Отступления от Архитектуры — вслух
до кода. В конце сессии — Журнал. Перед `/clear` дерево чистое и всё запушено.
```

#### Б. Этап 8 — вынос ядра в `FamilyCore` и перевод «Трапезы»

Сессия кода, по Плану. Планирование выноса закрыто 18.09.2026 решениями
Р-45…Р-51 — эта сессия их выполняет, а не пересматривает.

```
Проект «Трапеза» — учёт еды, третье приложение семьи. Работать в корне
репозитория `C:\Ф\Prognananda\Claude Projs\Еда\Trapeza`.

Прочитай `CLAUDE.md` и все файлы `docs/` перед началом. Работаем по
`docs/03-План.md`, Этап 8 — вынос ядра. Сначала план работ, код после
подтверждения.

Развилки выноса закрыты 18.09.2026, Р-45…Р-51; читай их целиком, прежде
чем предлагать что-либо своё. Коротко:
— ядро — публичный репозиторий `goigolden88/FamilyCore`, подмодуль git
  в `src/shared` (Р-46, Р-50);
— ядро не знает про приложение: фабрики `createDb(config)` и прочие,
  конфиг — `src/app/config.ts`, `model.ts` переезжает в `src/app/model.ts`,
  сборка — в `src/app/core.ts` (Р-47);
— состав ядра и что остаётся приложению — таблица в Р-48. Прогон
  `scripts/smoke.mjs` не выносится; названия вкладок не выносятся (Р-16);
— порядок: «Трапеза» первой, потом «Финансы», потом «Делу Время»,
  «Дневники» последними (Р-49);
— договор семьи переезжает в `FamilyCore`, метаприложение не планируется
  (Р-51).

Первая версия ядра делается из «Делу Время» с `d86f0aa` — `git archive`,
байт в байт, как есть, следом вынос конфига. Локальная копия:
`C:\Ф\Prognananda\Claude Projs\ТаймМенеджмент\DeluVremya`. **Её репозиторий
только читается**, как и «Дневники»: перевод обоих — отдельными сессиями
из их корней (Р-49, `06-Процесс`).

Порядок работы — шаги 1–3 Этапа 8 в Плане. Проверка перевода «Трапезы»
обязательна вся: `npm test`, `npm run build`, `npm run smoke` — итог по
коду выхода; `npm run smoke -- --data seed/trapeza-2026-09-17.json`;
деплой; человек открывает установленное приложение на телефоне **без
переустановки** — прежняя база обязана открыться с прежними записями.
`DB_NAME`, `SCHEMA_VERSION`, имена хранилищ и `V1_STORES` не меняются;
миграций в переводе нет. Перевод — отдельной веткой, одним коммитом;
откат — `git revert` (Р-49).

Что решается в этой сессии впервые и требует записи в `05-Решения.md`:
— **как `FamilyCore` нумерует свои решения.** Чужие «Р-NN» в комментариях
  ядра после переезда остаются без хозяина (Р-43, Р-49);
— **где проходит граница каркаса стилей** — каркас в ядро, свои экраны
  и акцент (Р-44) здесь; шов виден только глазами, проверяется прогоном
  и вёрсткой на телефоне;
— **где режется `notify.ts`** — механика в ядро, правило дня (Р-30)
  и тексты здесь.

Ключевое по хозяйству — см. передачу А ниже: прогон, порча в `git worktree`,
кириллица в heredoc, личные данные только в `seed/`, строка в `changes.ts`
на видимое изменение. Перевода человек не видит — строки «Что нового»
у него нет.

В конце сессии — Журнал, передача, коммит, дерево чистое **и в подмодуле
тоже**.
```

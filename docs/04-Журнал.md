# Журнал

Что сделано и в каком оно состоянии. Отвечает на вопрос «где мы сейчас».
Обновляется в конце каждой беседы. Новые записи сверху.

---

## 18.09.2026 — Планирование выноса ядра: Р-45…Р-51, кода нет

Сессия планирования по передаче Б. Кода не написано ни строки: тронуты только
документы.

**Первый вопрос сессии — «доделана ли „Трапеза“»** — задан, как велит Р-22,
и ответ оказался «нет»: учёт прожил один день, четыре хвоста открыты, норм
на телефоне нет. Цена выноса посреди месяца названа до выбора. Человек
ответил, что **«Финансы» начинаются прямо сейчас**, и выбрал выносить:
из двух условий Р-22 первым наступило «до четвёртой копии».

**Решения — записаны в момент выбора**

- **Р-45 — вынос начинается 18.09.2026.** Уточняет срок Р-22. Цена принята:
  починка общего становится релизом, два замороженных репозитория
  расконсервируются, хвосты «Трапезы» ждут
- **Р-46 — ядро раздаётся подмодулем git** из своего публичного
  репозитория. Отвергнуты npm-пакет, скрипт-сверка, монорепо — последнее
  ломает раздачу: Pages даёт один сайт на репозиторий. Рекомендация,
  принятая человеком
- **Р-47 — шов: фабрики с явным конфигом.** `createDb(config)` и прочие;
  `AppConfig` — в `src/app/config.ts`, `model.ts` переезжает в `src/app/`,
  сборка — в `src/app/core.ts`; подмодуль — в `src/shared/`. Отвергнуты
  алиас на конфиг и «малое ядро». Рекомендация, принятая человеком
- **Р-48 — состав ядра:** механика, общие `ui/`, работник с механикой
  напоминаний, экраны-инфраструктура, каркас стилей и сборки. Прогон
  `smoke.mjs` не выносится — выбор человека. Уточняет Р-16 в части
  `notify.ts`
- **Р-49 — эталон после выноса — репозиторий ядра;** порядок перевода
  «Трапеза» → «Финансы» → «Делу Время» → «Дневники»; правило «только
  читать» снимается лишь для перевода, сессиями из их корней; откат —
  `git revert` коммита-переезда, точка невозврата — первая миграция поверх
  ядра. Выбор человека в обоих вопросах
- **Р-50 — репозиторий ядра — `FamilyCore`, публичный.** Выбор человека;
  ряд имён семьи ради этого перестаёт быть однородным
- **Р-51 — договор семьи переезжает в `FamilyCore`;** контуры
  метаприложения не пишутся нигде, пока семья не собрана. Рекомендация,
  принятая человеком; ни один из трёх вариантов передачи не выбран —
  предложен четвёртый

**Разбор трёх диффов — главный результат сессии**

Считалось по объектам git, а не по файлам с диска.

- **Одинаково во всех трёх — 13 файлов:** `github.ts`, `id.ts` с тестом,
  `Fold`, `Install`, `ScrollButtons`, `backup`, `install`, `useFold`,
  `useSync`
- **«Дневники» отстали — 16 файлов:** `dates` (112 и 119 строк), `feed`
  (114), `report` (15) и девять файлов, которых у них просто нет. Это не
  расхождение по делу, а 17 невзятых коммитов
- **Разное по делу приложения:** `model.ts` (154 строки), `layout.test`
  (133), `db.test` (62), `layout` (38), `sync.test` (38), `importing` (33),
  `db` (24), `Layout.tsx` (18), `sw.ts` (12), `sync.ts` — **4**,
  `SyncSettings` — 3, `styles.css` (176)
- **Разное случайно — пусто.** Ни одной правки механики, разошедшейся между
  «Делу Время» и «Трапезой». Сводить перед выносом нечего

**Отсюда постановка задачи изменилась.** Р-06 предполагал расплетание
перемешанного кода. По трём диффам видно другое: частное свелось к пяти
параметрам — имя приложения, имя базы, хранилища с индексами, `PLACES`,
формат импорта, — плюс `SCHEMA_VERSION` с миграциями и свои правила промпта.
Вынос — это вынесение конфига, а не разделение логики. Заодно сам собой
закрылся пункт передачи про версии: `schemaVersion` оказался полем конфига,
и ядро его не диктует.

**Находка: часть `screens/` — тоже ядро.** `WhatsNew.tsx` совпадает байт
в байт (0 из 39), `useFirstRun` — 4 из 115, `useWhatsNew` — 5 из 55,
`useFeed` — 5 из 67, `ImportRecords` — 19 из 217, **`notify.ts` — 41 из
355**, хотя передача записывала его в «не выносится никогда». `tsconfig.json`
и `deploy.yml` совпадают целиком. Против: `Settings` — 690 из 695,
`Today` — 1053, `registry.ts` — 119 из 182: здесь Р-16 прав.

**Ошибка по ходу, моя.** Первую таблицу сравнения собрал по файлам с диска —
рабочая копия «Дневников» развёрнута в CRLF, и все их файлы выглядели
«иными». Поймал на диффе `db.ts`, где различия показались тотальными.
Пересчитал из git-объектов, где у всех трёх `eol=lf`; таблица в Журнале —
уже верная. Ловится теперь: сравнение копий — только через `git show`,
никогда по рабочим файлам.

**Сделано** — документы, кода нет

- `05-Решения.md` — Р-45…Р-51; список открытых вопросов переписан
- `02-Архитектура.md` — «Договор семьи» сокращён до того, чем «Трапеза»
  обязана, и отослан в `FamilyCore`; добавлено целевое дерево «После
  перевода на `FamilyCore`»; правила пополнены двумя строками; слабые места
  — про подмодуль
- `01-Проект.md` — «Соседние проекты»: ядро, договор, «Финансы»,
  метаприложение
- `03-План.md` — **Этап 8 «Вынос ядра в `FamilyCore`»** шагами 1–4
  с проверкой и откатом; «Отложено» переписано; отдельным разделом — вопрос
  про «Дачу», который задаётся до кода
- `06-Процесс.md` — `FamilyCore` в таблице «Где что живёт», эталон, правило
  «только читать» с единственным исключением, новый раздел «Подмодуль ядра»;
  передача Б заменена на сессию кода Этапа 8
- `CLAUDE.md` — эталон и правило правки ядра

**Состояние: планирование выноса закрыто, кода нет**

*Проверено машинно* — `git status` чистый после коммита; Р-01…Р-51 подряд,
без пропусков; тесты и сборка не гонялись — `src/` не тронут

*Проверено на устройстве* — нечего: кода нет

**Что сказал человек по ходу, не решением**

- **«Финансы» — на этой неделе**, дальше приложение для дачи, «считай
  две недели и буду делать мета-прилку». Замысел «Дачи» ещё не сложился,
  войдёт ли она в семью — решит позже
- **Обязывать «Дачу» ядром заранее — отказался,** и довод верный: это
  решение об архитектуре приложения, которого ещё нет. Записано не решением,
  а вопросом в Плане
- Сказано ему в ответ: за две недели метаприложение сможет сводную ленту
  и общий поиск, но не связи вроде «ел не то — заболел» — для них нужны
  месяцы; и что Этап 8 теперь стоит на пути у «Финансов», а не рядом с ними

**Не описано в документах — на обсуждение**

- **Нумерация решений в `FamilyCore`** и **граница каркаса стилей с местом
  разреза `notify.ts`** — оба в открытых вопросах `05`, решаются при
  заведении ядра и при переводе
- **Кто эталон для `TrapezaData`-подобных репозиториев данных** — README
  данных собирается ядром из `name` и `storeNotes`; текст у трёх приложений
  разный, сведение не обсуждалось
- Прежние: история норм по всем неделям и «как вчера» без строки норм —
  оба ждут первых норм на телефоне

**Следующий шаг**

`/clear` — и новая сессия по **передаче Б** в `06-Процесс`: Этап 8, шаги 1–3
— завести `FamilyCore`, вынести конфиг, перевести «Трапезу» веткой. Хвосты
«Трапезы» (передача А) ждут за ним: нормы на телефоне, напоминание, рецепт
у плиты.

---


## 18.09.2026 — Хвосты и «на обсуждение» разобраны: Р-40…Р-44, кода почти нет

**Решения** — записаны в момент выбора, `b7eb691`

- **Р-40 — «Блюда» остаются вкладкой нижней панели;** срок 02.10 из Р-36 снят. Слова человека: «по „Блюдам“ думаю, уже можно гарантированно оставить». Ответ пришёл раньше срока и не из счёта заходов, а из Этапа 6: рецепт живёт в форме блюда (Р-39), и «Блюда» — единственное место, где он вводится и читается
- **Р-41 — `share_target` не объявляется;** пункт снят с обсуждения, где висел с 15.09. Сказано до выбора: это не строка манифеста, а экран приёма — присланный текст безымянен, приложение не знает, к какому блюду его класть; объявление вдобавок зашивается в установленное приложение навсегда, как ярлык (Р-16). Оплачивал бы это путь, которым человек не прошёл ни разу. Возврат — по условию: перенос ответа ИИ в поле блюда окажется утомительным. Рекомендация, принятая человеком
- **Р-42 — шаблон применяется с архивным блюдом.** Вопрос человека «в чём проблема». Разобрано: оба молчаливых поведения плохи одинаково — поставить архивное или тихо укоротить шаблон; разница в том, чья это воля. Архив управляет тем, что предлагают, и не переписывает список, собранный руками. Рекомендация, принятая человеком
- **Р-43 — три оговорки к правилам, кода не требуют:** прогон ходит в IndexedDB сам из `scripts/`; список тем напоминания и `combineResults` оставлены мёртвыми ради диффа к `d86f0aa`; голый «Р-NN» в файлах ядра — решение «Делу Время»
- **Р-44 — акцент экранов тёплый при зелёной иконке,** так и задумано. Слова человека: «по интерфейсу всё ок»

**Находка по ходу — номера решений пересеклись с ядром.** `core/dates.ts` говорит «неделя — с понедельника (Р-41)», и это **их** Р-41; свой Р-41 появился сегодня. Названо человеку до записи решений. Переписывать ссылки в ядре отвергнуто: дифф к `d86f0aa` растёт ради комментариев, а платить за это выносом ядра (Р-22). Вместо правки — правило чтения в `06-Процесс` и Р-43.

**Сделано** — три коммита

- `cc6b07a` — `styles.css`: комментарий у `--accent` переписан под правду. Был перенесён из «Делу Время» дословно — «в тон иконке… два приложения», а у нас иконка зелёная (`scripts/icons.mjs`) и приложений три. Цвета не тронуты; строки в `changes.ts` нет — человеку правка не видна
- `b7eb691` — Р-40…Р-44; План: хвост «Блюда» снят, четыре оставшихся уточнены словами человека от 18.09
- `287f9c9` — Журнал, `06-Процесс`: оговорка про прогон и IndexedDB, правило чтения «Р-NN» в ядре, передача переписана
- Этот коммит — **передач стало две.** По просьбе человека: **А** — хвосты и починка, с порядком разбора поломки (воспроизвести, назвать причину, проверка до правки, `changes.ts`, прогон, две другие копии ядра); **Б** — сессия планирования выноса ядра и контуров метаприложения. Обе в `06-Процесс`

**Состояние: на сайте ничего нового; работы по коду в Плане нет**

*Проверено машинно* — из-за правки `src`, хоть и комментария

- `npm test` — код 0, 491 тест
- `npm run build` — код 0
- `npm run smoke` — код 0, 96 проверок
- Порчей прогон не проверялся: сценарий не менялся

*Проверено на устройстве* — ничего

**Хвосты — слова человека 18.09:** шаблоном завтрака и «Как обычно?» не пользовался, «расскажу позже»; норм всё ещё нет; напоминания не видел, заполнять не забывает — попробует нарочно пропустить день, учёта прожит один день; рецепт проверит в настоящей готовке. Хвост «Блюда» закрыт Р-40, осталось четыре.

**Не описано в документах — на обсуждение** — осталось три: история норм по всем неделям; «как вчера» без строки норм (Р-25) — оба ждут первых норм на телефоне; вынос ядра (Р-22) — когда «Трапеза» доделана. Пять прежних пунктов закрыты Р-40…Р-44.

**Следующий шаг**

`/clear` — и новая сессия по **передаче А** в `06-Процесс`: хвосты и починка. Кода в очереди нет. Первое, чего ждём от жизни: нормы на телефоне — без них стоят три пункта разом; готовка по рецепту 19.09 — её итог решает судьбу `share_target` (Р-41).

**Передача Б — вынос ядра — отдельной сессией и позже.** Повод назвал человек: следующее приложение семьи — «Финансы», а Р-22 требует вынести ядро до их планирования. Вместе с выносом там же разбираются контуры метаприложения: здесь, в своём репозитории или пока нигде. Сказано человеку до записи и повторено в самой передаче: **срок Р-22 — «когда „Трапеза“ доделана», а она прожила один день учёта** при четырёх незакрытых хвостах. Выносить ядро посреди месяца, который ещё поменяет `core` и `sw.ts`, — резать по диффу, который поедет. Выбор — за человеком, в той сессии, первым её вопросом.

---

## 18.09.2026 — Этап 6 закрыт человеком

**Решения**

- Этап 6 закрыт словами человека «всё проверил, работает»; из Плана убран вместе с Этапами 3–5
- Работы по коду в Плане не осталось: новая берётся из ответов человека на хвосты, из «на обсуждение» и из жизни
- **Рецепт у плиты** — не решение, а хвост: рецепт читается только внутри формы блюда. Перенесён из «на обсуждение» в хвосты Плана, проверяется первой готовкой по нему

**Проверено на устройстве** — человеком, на телефоне: код Р-37 и Этапа 6 работает. Хвосты прежних этапов остаются

**Не описано в документах — на обсуждение** — прогон читает IndexedDB напрямую из `scripts/`; список тем напоминания ради диффа к ядру; шаблон с архивным блюдом; акцент интерфейса при зелёной иконке; история норм по всем неделям; «как вчера» без строки норм; `share_target`; вынос ядра (Р-22)

**Следующий шаг**

`/clear` — и новая сессия по передаче в `06-Процесс`. Кода в очереди нет: сессия начинается с вопросов человеку по хвостам и с разбора «на обсуждение». Этап 7 «Отдать другу» отложен до готовности семейства приложений.

---

## 17.09.2026 — Р-37 и Этап 6: рецепт блюда текстом; код на сайте, ждёт телефона

**Решения** — записаны в момент выбора, `d9853d0`, `0088440`

- **Р-38 — рецепты сейчас, срок «месяц учёта» из Р-07 снят.** Вопрос человека «почему надо ждать месяц, что мешает сделать сейчас». Сказано до выбора: технического повода ждать нет, а довод Р-07 «месяц покажет, у каких блюд оценки не хватает» закрыт историей февраля–марта; ждать стоило бы только ради проверки, смотрят ли на калории вообще. Человек: калории как сейчас — у одних есть, у других нет, «время покажет». Выбор человека
- **Р-39 — рецепт: один текст по заготовке в поле блюда; состава ссылками и расчёта калорийности нет.** Отменяет в Р-07 состав и расчёт. Развилки названы до кода: ссылка на ингредиент по названию рвётся при переименовании (id у блюда не меняется — проверено в `applyDish`), неполный состав занижал бы итог дня, выход надо взвешивать. Человек выбрал текст-заметку. Отдельный словарь рецептов — отвергнут вслух: новая сущность, миграция, свой файл синхронизации, а поле у блюда уже есть. Рекомендация, принятая человеком

**Сделано** — три коммита, запушено, деплой `af32f75` — `success`

- `d9853d0` — Р-38, План: срок этапа снят
- `44631d3` — Р-37: `SEARCH_FROM` снят, поиск в приёме всегда; пустой справочник больше не прячет приёмы — подсказка стоит над ними, первое блюдо заводится поиском; строка 21
- `0088440` — Р-39, Архитектура (`recipe?: string`, тип `Recipe` убран, `recipe.ts` в структуре), `01-Проект`, План Этапа 6
- `af32f75` — `modules/food/recipe.ts` с тестами: заготовка, промпт для ИИ, `cleanRecipe` (пустая заготовка — не рецепт); `recipe` в `DishInput`, `DishChanges`, `applyDish`, `createDish`; блок «Рецепт» в форме блюда с кнопкой «Заготовка» и промптом, пометка «· рецепт» в списке; вопрос справки; строка 22

**Находки по ходу**

- **Сторож справки поймал «100 г» в новом абзаце** — переписано на «поле калорийности того же блюда»
- **Блок «Рецепт» есть и в «Новом блюде», и в форме правки** — у них были одинаковые id сворачивания, и прогон писал рецепт не в то поле: id разведены `dishes:new:recipe` и `dishes:edit:recipe`, проверки прогона привязаны к `.cat__body`
- **Heredoc снова съел `\n`** — в тексте рецепта для прогона; собран списком строк через `join`
- Имя `saved` в прогоне уже было занято — прогон не запустился вовсе, пока не переименовал

**Состояние: Р-37 и Этап 6 на сайте; этап не закрыт — ждёт человека**

*Проверено машинно*

- `npm test` — код 0, 491 тест (было 480)
- `npm run smoke` — код 0, 96 проверок: поиск в приёме на пустой базе и при трёх блюдах с «Завести и записать»; рецепт — заготовка кнопкой, текст сохраняется и читается после закрытия формы, пометка в списке, промпт зовёт блюдо по имени и просит оценку ккал; справка говорит, что ккал из рецепта не считаются
- `npm run smoke -- --data seed/trapeza-2026-09-17.json` — код 0, 17 проверок
- Порча по одной на прогон: порог поиска обратно — код 1, не сошлись 2 проверки; пометка «· рецепт» снята — код 1, 1; `cleanRecipe` всегда «рецепта нет» — код 1, 1. Рабочая копия убрана, переход снят
- Деплой `44631d3` и `af32f75` — `success`

*Проверено на устройстве* — ничего

*Не проверено*

- Рецепт на телефоне: как вводится с экрана, читается ли у плиты, как ложится текст ИИ в поле
- Промпт целиком в чате с ИИ — прогон читает его только на экране

**Не описано в документах — на обсуждение**

- **Рецепт виден только внутри формы блюда** — чтобы читать у плиты, нужно открыть «Блюда», найти блюдо, развернуть форму. Отдельного показа «только чтение» нет; надо ли — покажет готовка
- Прежние — прогон читает IndexedDB напрямую из `scripts/`; список тем напоминания ради диффа к ядру; шаблон с архивным блюдом; акцент интерфейса при зелёной иконке; история норм по всем неделям; «как вчера» без строки норм; `share_target`; вынос ядра (Р-22)

**Следующий шаг**

Человек на телефоне: рецепт борща — заготовка, промпт, вставка ответа ИИ, перенос оценки ккал в поле блюда; поиск в приёме при коротком списке. Подтвердит — Этап 6 закрывается; дальше хвосты Плана и Этап 7.

---

## 17.09.2026 — Этап 5 закрыт человеком

**Решения**

- Этап 5 закрыт словами человека «всё работает»; из Плана убран
- **Р-37 — поиск в выборе приёма при любом числе блюд;** первым пунктом следующей сессии кода. Выбор человека: «странное ограничение — снять, отложить на следующие этапы». Закрыт пункт «на обсуждение» о пороге в 12 блюд
- Пункт «часы компьютера — 17.09» снят: человек подтвердил дату, расхождения нет

**Проверено на устройстве** — человеком, на телефоне: код Этапа 5 работает. Хвосты прежних этапов в Плане остаются

**Не описано в документах — на обсуждение** — прогон читает IndexedDB напрямую из `scripts/`; прежние: список тем напоминания ради диффа к ядру, шаблон с архивным блюдом, акцент интерфейса при зелёной иконке, история норм по всем неделям, «как вчера» без строки норм, `share_target`, вынос ядра (Р-22)

**Следующий шаг**

`/clear` — и новая сессия по передаче в `06-Процесс`: Р-37 первым делом; Этап 6 «Рецепты» — не раньше 18.10.2026 (Р-07), до того сессия заканчивается на Р-37 и хвостах.

---

## 17.09.2026 — Этап 5: код ленты, markdown, справки и первого запуска на сайте; ждёт проверки на телефоне

**Решения** — записаны в момент выбора, `b996590`, `8797e85`

- **Р-32 — «Как обычно?» развёрнут по умолчанию, свёрнутость помнится один день** — ключ `today:usual:<дата>`. Рекомендация, принятая человеком
- **Р-33 — вход в ленту — ⌕ в шапке «Сегодня», не вкладка.** Рекомендация, принятая человеком
- **Р-34 — markdown: период на выбор, разделов нет, пока вид один.** Отступление от формулировки Плана «разделы и период» — сказано до кода. Рекомендация, принятая человеком
- **Р-35 — строки «4» и «5» Р-16 дополнены:** блок «Напоминания» из их `Settings.tsx` (закрыт пункт «на обсуждение» Этапа 4), `useRecordDates`, `period.test`, `monthFold.test`. Выбор человека
- **Р-36 — «Блюда» остаются экраном; новое блюдо — из поиска в выборе приёма.** Вопрос человека «зачем „Блюда“, объединить с „Рецептами“». Сказано до выбора: экрана рецептов в документах нет — рецепт поля блюда (Р-07); на «Сегодня» справочник не правится. Настоящая дыра — новое блюдо требовало ухода на вкладку: п. 6 Этапа 5. Убирать ли вкладку — через две недели учёта. Рекомендация, принятая человеком

**Хвосты — слова человека:** обычный день — «чуть больше минуты» вчера и сегодня, шаблоны не использовались; норм не заводил; напоминания включены, «Проверить сейчас» сработало — фоновое пробуждение не подтверждено. Хвосты в Плане уточнены, не сняты

**Сделано** — тринадцать коммитов, запушено, деплой `afa1c03` — `success`; в сборке на сайте — «Markdown для чтения», «Завести «», `today:usual:`, «Лента и поиск»

- `b996590`, `8797e85` — Р-32…Р-36, Архитектура (`installNoteHidden`, входы ленты и справки, строка ленты, новое блюдо из выбора), План
- `3218ce7` — `food/feed.ts`: строка ленты на день — приёмы с порциями, порции и ккал с основанием, в поиске категории, группы, заметки; раздел markdown — месяц с итогом, день, приёмы подпунктами, кривые даты в конце; реестр — `feed`, `markdown`, `feedItems`, `markdownExport`
- `f9177d5` → `4807a5a` — лента: `Feed`, `useFeed`, `monthFold` с тестом как есть (хеши сверены, собирается) → тексты, счёт днями, без подписи вида при одном виде, маршрут, ⌕; строка 15
- `dd26c27` → `4442181` — markdown: `period` с тестом, `useRecordDates` как есть (хеши сверены, не собирается) → из `period` только выбор периода, свои тесты `monthChoices`, `yearChoices`, `exportSpan`; `useRecordDates` на записях еды; блок в «Настройках»; строка 16
- `23b00d5` → `6701d76` — справка: `Help`, `help.test` как есть (хеши сверены, не собирается) → тринадцать вопросов «Трапезы», числа из констант; `hourText`, `shareText`, `monthsText` в `labels.ts` с тестом; «?»; строка 17
- `82f8ce0` → `b5f6be9` — первый запуск: `Welcome`, `useFirstRun`, `useWhatsNew` как есть (хеши сверены, не собирается) → своё приветствие под сторожем цифр, окно «Что нового» и строка про iPhone на «Сегодня»; строка 18
- `480fc5f` — `usualFoldId`, `waitingText` с тестами; «Как обычно?» — `Fold`; строка 19
- `afa1c03` — `newDishOffer` с тестами; «Завести „…“ и записать», из архива — «Вернуть и записать»; строка в справке; строка 20
- Этот коммит — Журнал, План (что проверяет человек), `01-Проект` (лента, markdown, справка, новое блюдо)

**Находки по ходу**

- **Ревизия сворачивания:** на «Неделе», «Блюдах» и в «Настройках» сворачивается уже всё; новых кандидатов, кроме «Как обычно?», месяцев ленты и вопросов справки, нет
- **Первый «помощник» счёта ленты врал после «из»** — «Показано 0 из 1 день»; пойман до сборки чтением, заменён склонением после «из»
- **Heredoc снова съел `\n`** в шаблонных строках `markdownExport` — поправлено инструментом правки до тестов
- В справке первые примеры были цифрами — «3 из 5 дней», «по 5 из 7 записей»: переписаны словами до сторожа
- `help.test` у них проверяет и `Welcome.tsx` — в паре справки его ещё не было: сторож на время пары только на `Help`, `Welcome` вернулся под сторож своей парой
- Прогон читает числа справки из исходников констант — справка и прогон не расходятся при правке константы
- Порча ленты дала три несошедшиеся проверки, а не одну: следующие шаги сценария стоят на найденной строке

**Состояние: код Этапа 5 на сайте; этап не закрыт — ждёт человека**

*Проверено машинно*

- `npm test` — код 0, 480 тестов
- `npm run build` — код 0; `npm run smoke` — код 0, 92 проверки: приветствие «Трапезы» на пустой базе, «Понятно» переживает перезапуск, «Что нового» свежей установке не показано; лента по ⌕, счёт днями, старые месяцы свёрнуты; «сладости 21 февраля» — одна строка «Обед: Торт ×2, Кисель», «3 порции · 80 ккал по 1 из 2 записей», тап — день, сегодня — `#/`; markdown файлом — «- 21.02, сб — 3 порции» и приём подпунктом, без чипов; за март — «Период: март 2026.», других месяцев нет; справка по «?», тринадцать вопросов свёрнуты, числа шести констант совпали с исходниками; «Что нового» на копии без `seenChanges` — последняя запись, не предпоследняя; «Как обычно?» развёрнут «· 4 приёма», свёрнутый без строк переживает перезагрузку, в `folds` один ключ с сегодняшней датой; четыре тапа — прежние
- `npm run smoke -- --data seed/trapeza-2026-09-17.json` — код 0, 17 проверок: `/feed` и `/help` развёрнуты без ошибок; лента — 47 дней, «март 2026» — 19 из 47; «Завести «Пицца прогона» и записать» — запись в завтраке, блюдо в «Без категории», при найденном кнопки нет
- Порча в `git worktree`, по одной на прогон: лента без категорий в поиске — код 1, 3 проверки; markdown без отбора периода — код 1, 1; ключ «Как обычно?» без даты — код 1, 1; «Завести» при найденном — код 1, 1 (`--data`). Рабочая копия убрана, переход снят
- Поиск `indexedDB` в `src` вне `core/db.ts` и тестов — пусто
- Деплой `afa1c03` — `success`

*Проверено на устройстве* — ничего

*Не проверено*

- «Назавтра „Как обычно?“ развёрнут» — только тестом ключа: часы прогона не переводятся
- «Завести и записать» — только на копии: на основном прогоне блюд меньше порога поиска
- Вёрстка шапки «Сегодня» с тремя значками и ленты на узком экране — только прогоном
- «Поделиться markdown» на телефоне — прогон нажимает только «Сохранить»

**Не описано в документах — на обсуждение**

- **Поиск в выборе приёма появляется с 12 живых блюд** (`SEARCH_FROM` в `Today.tsx`, с Этапа 1) — меньше блюд — нет поиска, и «Завести и записать» недоступно. У человека 77 блюд; задевает новичка с короткой базой — ему остаются «Блюда»
- **Часы компьютера — 17.09.2026,** а человек говорит о двух днях учёта, начатого 18.09: даты решений и Журнала — по часам компьютера
- **Прогон снимает `seenChanges` и читает `folds` прямо из IndexedDB** — в `scripts/`, как прогон «Делу Время»; правило «только через `core/db.ts`» записано для кода приложения
- Прежние — список тем напоминания ради диффа к ядру, шаблон с архивным блюдом, акцент интерфейса при зелёной иконке, история норм по всем неделям, «как вчера» без строки норм, `share_target`, вынос ядра (Р-22)

**Следующий шаг**

Человек на телефоне — список в Плане, Этап 5: «Что нового» после обновления, лента и поиск дня, справка глазами, свернуть «Как обычно?», новое блюдо из поиска, markdown за февраль. Подтверждено — Этап 5 закрывается, передача в `06` переписывается под Этап 6 «Рецепты» (не раньше месяца учёта, Р-07); `/clear`.

---

## 17.09.2026 — Этап 4 закрыт человеком

**Решения**

- Этап 4 закрыт словами человека «в целом всё норм… всё проверил — работает»; из Плана убран
- **Р-31 — «Как обычно?» сворачивается; ревизия сворачиваемых блоков — пунктом 5 Этапа 5.** Выбор человека; отменяет в Р-29 «не складывается». Развёрнут ли блок по умолчанию — до кода Этапа 5

**Проверено на устройстве** — человеком, на телефоне: код Этапа 4 работает. Словами не названо отдельно — хвостами в Плане: обычный день за минуту и в четыре тапа на днях учёта, приход напоминания самого

**Не описано в документах — на обсуждение** — пункты записи ниже: блок «Напоминания» сверх строки «4» Р-16, список тем напоминания, шаблон с архивным блюдом; прежние — акцент, история норм по всем неделям, «как вчера» без строки норм, `share_target`, вынос ядра (Р-22)

**Следующий шаг**

`/clear` — и Этап 5 «Лента, выгрузка, справка» новой сессией по передаче в `06-Процесс`.

---

## 17.09.2026 — Этап 4: код повтора приёма на сайте; ждёт проверки на телефоне

**Решения** — записаны в момент выбора, `df50e39`, `a1fe3c9`, `26c6e18`

- **Р-26 — выбор блюда в приёме: «Частые» сверху, дальше все блюда по категориям, свёрнутыми.** Рекомендация; человек настоял на категоризации. Ручного порядка блюд нет — «по Д согласен»; дописано в Р-26 в той же сессии до кода, сказано в записи
- **Р-27 — порядок норм стрелками на «Неделе»,** не в «Настройках» — рекомендация, принятая человеком
- **Р-28 — шаблоны приёма и дня одной записью `Template`;** шаблон дня сегодня пишет только начавшиеся приёмы. Шаблоны дня — просьба человека; модель и «только начавшиеся» — рекомендации. `MealTemplate` в Архитектуре переименован, миграции нет
- **Р-29 — «Как обычно?»: шаблон приёма, приём из шаблона дня, иначе блюда хотя бы в половине последних двадцати таких приёмов.** Предложение человека «самый частый набор», проверенное на истории: точный набор случаен (21 разный за 47 завтраков), блюда по отдельности устойчивы. Моя первая рекомендация — прошлый приём не старше недели — отвергнута как «рандом». «Не было» — настройка устройства `usualSkipped`, в Архитектуре
- **Р-30 — напоминание: сегодня ничего не записано или вчера без завтрака, обеда или ужина.** Человек хотел проверку трёх приёмов «в конце дня»; время выбирает браузер — отсюда вчерашний день. Закрыт открытый вопрос «Незаполненный день»
- Хвост «Блок „Нормы недели“ не виден» снят: норм на телефоне нет, сборка актуальная — слова человека

**Сделано** — одиннадцать коммитов кода и документов, запушено

- `df50e39`, `a1fe3c9`, `26c6e18` — Р-26…Р-30, Архитектура (`Template`, `usualSkipped`, заметки), План
- `e2335fb` — `food/picker.ts`: «Частые», разделы категорий, поиск по разделам; `mealFrequency` вынесена из `byFrequency`; строка 10
- `5a8489e` — `food/templates.ts`: из записей, название с «Заменить состав», применение без дублей с «на потом», сдвиг внутри вида; `startedMeals`; слияние блюд в шаблоне дня — повтор только в том же приёме
- `9c7d832` — шаблоны на «Сегодня» и блок «Шаблоны» на «Блюдах»; строка 11
- `6f624fa` — `food/usual.ts` и блок «Как обычно?»; строка 12
- `a224ecc` — `moveNorm`, стрелки на «Неделе»; строка 13
- `7fc68f5` — `notify.ts`, `notify.test.ts`, `sw.ts` с `d86f0aa` как есть; blob-хеши трёх сверены, сборка на коммите не проходит
- `3d00eb6` — правки: `food/remind.ts`, одна тема, тап на «Сегодня», блок «Напоминания» из их `Settings.tsx`; строка 14
- `1b850bf` — ярлык: нового маршрута не нужно, комментарий и проверка прогона
- Этот коммит — Журнал, План, `01-Проект` (повтор приёма и напоминание)

**Находки по ходу**

- **У текущего приёма был открыт список из 77 блюд, и всё ниже него уезжало вниз** — вероятная причина, почему блок норм «не виден» и при нормах. Р-26 это убирает
- **Посчитано на истории:** обычный обед без шаблона — только компот: суп разошёлся на «Суп особый» 9 и «Суп обычный» 5 из 20. Записано ценой Р-29
- «Как вчера» с «Сохранить как шаблон» одним классом кнопки — старая проверка «кнопка ушла» считала и её. Своя `meal__save`
- Блюдо «БОРЩ» после слияния с другим устройством — «Борщ»: проверки шаблонов искали старое имя
- В `run` прогона нет помощников — `byText` там `ReferenceError`; помощники — только в `act`
- Проверка «Не было» сначала зависела от часа и ночью не выполнялась; переделана — удалённый вчерашний ужин возвращает вопрос
- **Работник вырос с 16 до 27 КБ:** `notify` тянет `usual` → `templates` → `forms` → `import`. У «Делу Время» так же тянется их модуль
- Два прогона разом делят порт отладки — порча и основной прогон шли по очереди
- Heredoc Bash с большим TSX снова споткнулся — правки шли скриптами-файлами из папки сессии

**Состояние: код Этапа 4 на сайте; этап не закрыт — ждёт человека**

*Проверено машинно*

- `npm test` — код 0, 455 тестов в 32 файлах
- `npm run build` — код 0; `npm run smoke` — код 0, 81 проверка: категории выбора по порядку и свёрнуты, разворот тапом, «Частые» по частоте; шаблон приёма и дня из записанного, «Заменить состав», применение без дублей на прошлом дне, сегодня без не начавшихся приёмов, состав на «Блюдах»; «Как обычно?» — шаблон, приём из шаблона дня, «Как обычно: Торт — по 11 ужинам»; обычный вчерашний день — четыре тапа; о сегодняшнем — до текущего приёма; «Не было» переживает перезагрузку; норма стрелкой первая на «Неделе» и «Сегодня»; «Проверить сейчас» с «Не было» — напоминать не о чем; ярлык открывает список текущего приёма
- `npm run smoke -- --data seed/trapeza-2026-09-17.json` — код 0, 13 проверок: на копии «Частые» — 8, в категориях 77 из 77, поиск «Найдено 39 из 77» под 19 заголовками
- Порча в `git worktree`, по одной на прогон: «Частые» без отбора — код 1, 2 проверки; шаблон без проверки дублей — код 1, 3; «Как обычно?» мимо шаблона приёма — код 1, 1; сдвиг нормы впустую — код 1, 1; напоминание без «Не было» — код 1, 1. Рабочие копии убраны, переходы сняты
- Поиск `indexedDB` вне `core/db.ts` и тестов — пусто
- Деплой `1b850bf` — `success`; `7fc68f5` без сборки отдельно не деплоился — ушёл одним пушем с правками

*Проверено на устройстве* — ничего

*Не проверено*

- **«Четыре тапа» в реальном времени** — прогон записывает вчерашний день через «Как обычно?» и отклонение на сегодня; день по часам на телефоне — человек, 18.09
- Приход напоминания само — только на Android у установленного, днями
- Ветки «о сегодняшнем» прогон прошёл только для завтрака: запускался ночью. Остальные часы — тестами `unansweredMeals`
- Вёрстка «Как обычно?», шаблонов и стрелок норм на узком экране — только прогоном

**Не описано в документах — на обсуждение**

- **Блок «Напоминания» из их `Settings.tsx`** взят вместе с `notify.ts`, а в таблице Р-16 строка «4» называет только `notify.ts` и `sw.ts`. Сказано в Плане и коммите; решением не записано
- **Тема напоминания одна, но список тем и `combineResults` оставлены,** как у них: дифф к `d86f0aa` меньше для выноса ядра (Р-22)
- **Шаблон с блюдом из архива** применяется и предлагается — архивное блюдо не удалено. Не решалось
- Прежние — акцент интерфейса при зелёной иконке, история норм по всем неделям, «как вчера» без строки норм, `share_target`, вынос ядра (Р-22)

**Следующий шаг**

Человек на телефоне, 18.09 — первый день учёта: выбор блюда, свой шаблон завтрака и дня, «Как обычно?», «Напоминания» → «Включить» и «Проверить сейчас», ярлык. Подтверждено — Этап 4 закрывается, передача в `06` переписывается; `/clear` и Этап 5 новой сессией.

---

## 17.09.2026 — Этап 3 закрыт человеком

**Решения**

- Этап 3 закрыт словами человека «в целом всё норм»; из Плана убран. Учёт в реальном времени — с 18.09.2026
- Ручной порядок норм — пунктом 6 Этапа 4. Человек предложил «в настройках»; сказано до выбора: «Настройки» — место настроек устройства, они не синхронизируются, а порядок нормы — поле синхронизируемой записи. Рекомендация — стрелками на «Неделе», как категории; развилка — до кода Этапа 4

**Находки человека**

- **Не видит блок «Нормы недели» на «Сегодня».** Проверено: сборка на сайте (`index-DHmWY3Ws.js`) блок содержит; единственное условие показа — хоть одна норма на устройстве, записи не нужны; блок свёрнут и стоит между приёмами и «Итогом дня». Причина не найдена — хвостом в Плане, первым делом Этапа 4. Версии: норм на этом устройстве нет (заведены на другом, синхронизация не прошла), открыта старая сборка, блок не узнан в свёрнутом виде

**Не описано в документах — на обсуждение** — прежние пункты записи ниже: акцент интерфейса, история по всем неделям, «как вчера» без строки норм; `share_target`; незаполненный день — в Этапе 4; вынос ядра (Р-22)

**Следующий шаг**

`/clear` — и Этап 4 «Повтор приёма» новой сессией по передаче в `06-Процесс`.

---

## 17.09.2026 — Этап 3: код «Недели» и норм на сайте; ждёт проверки на телефоне

**Решения** — записаны в момент выбора, `be67ff6`

- **Р-23 — отклик норм не красится нигде;** словами и числом: «3 из 5 дней», «3 дня при пределе 4», ✓ у ясно выполненной. Закрыт открытый вопрос «Тон отклика норм». Предел на «Сегодня» остаётся — расходимся с их Р-45. Рекомендация, принятая человеком
- **Р-24 — неделя в счёт истории, когда исход ясен;** день без записей неизвестен. Повод — разрыв учёта: правило «Делу Время» засчитало бы полгода пустых недель. Рекомендация, принятая человеком. **Уточнено по ходу кода, до пуша, отдельным коммитом `c7f96f8`:** неделя без единой записи не называется неясной — из сентября «27 недель не ясны» было бы шумом. Правка записи, сделанной в той же сессии, — сказано здесь, чтобы не выглядело переписанным задним числом
- **Р-25 — экран «Неделя»,** нормы заводятся на нём (выбор человека); на «Сегодня» — «Нормы недели» и строка после тапа. История — по всем неделям в счёт, а не за четыре последние
- Режим полёта — подтверждён человеком, из хвостов снят. План работ — «приступай»

**Сделано** — девять коммитов, запушено, деплой `dd9dfbd` — `success`

- `be67ff6`, `c7f96f8` — Р-23…Р-25, Архитектура (заметка о норме), План
- `215e25f` — `ui/BarChart.tsx`, `ui/bars.ts` с тестом — `git archive d86f0aa`, blob-хеши трёх сверены. Пары «правки» нет: править было нечего (см. находки)
- `032ca46` — `food/norms.ts`: индекс дней учёта, итог недели по ясному исходу, история, задетые нормы, форма нормы; тексты норм в `labels.ts`
- `58f0f5c` — `food/week.ts`: дни, состав с числом дней, приёмы по дням, калории в среднем за день учёта с основанием
- `3d4241e` — «Неделя»: вкладка, `#/week?w=`, нормы с историей и малыми столбиками, форма нормы, порции по дням столбиками с переходом в день, состав, приёмы, калории; «Что нового» — строка 8; прогон — 8 проверок
- `1c0a8b1` — «Сегодня»: «Нормы недели» дня, задетые нормы в строке после тапа, «день уже в счёте»; строка 9; прогон — 3 проверки
- `2ddcdfa` — тест истории: норма над всеми категориями на файле таблицы — 6 полных недель в счёт, неполные по ясному исходу
- `dd9dfbd` — прогон `--data`: маршруты «Недели» и прошлого дня, норма формой на истории
- Этот коммит — Журнал и План

**Находки по ходу**

- **Копия для `--data` уже лежала в `seed/`** — `trapeza-2026-09-17.json`, 16.09: Журнал говорил «копии нет». Хвост закрыт прогоном
- **Правило истории «Делу Время» на нашем разрыве врёт** — отсюда Р-24; их окно «последние 4 недели» из сентября пусто — отсюда история по всем неделям
- **Акцент интерфейса — тёплый, как у «Делу Время»**, `--accent` и `--chart` `#b3620b`; зелёная у «Трапезы» только иконка (Журнал Этапа 0). Перекрашивать график одного — «заодно», не трогал; на обсуждение
- Прибавка порции к записи сначала давала «день не в счёте»: индекс «до» строился без прежней версии записи. Поймал тест до коммита
- Прогон по вставке шаблонной строкой из heredoc снова потерял экранирование `\n` — блок положен инструментом правки
- В одном коммите «Недели» и «Сегодня» были общие файлы: в индекс «Недели» положены версии `styles.css`, `smoke.mjs`, `changes.ts` без части «Сегодня» через `hash-object` и `update-index`

**Состояние: код Этапа 3 на сайте; этап не закрыт — ждёт человека**

*Проверено машинно*

- `npm test` — код 0, 395 тестов в 27 файлах; тест истории выполнен на файле — 4 из 4
- `npm run build` — код 0; `npm run smoke` — код 0, 64 проверки: «Неделя» вкладкой; норма формой; провал недели «5 дней при пределе 4» без ✓; «выполнена в 2 из 3 недель»; у нормы нет цвета, кроме текста и серого; выполненная — с ✓ и «пока 2»; семь столбиков с переходом в день; «Учёт в 7 днях из 7», состав с днями, «В среднем 80 ккал в день — за 7 дней учёта, по 7 из 12 записей»; приёмы по дням; на «Сегодня» строка нормы 5 → 6 сразу после записи; строка после тапа; «день уже в счёте»
- `npm run smoke -- --data seed/trapeza-2026-09-17.json` — код 0, 11 проверок: копия загружена (527 записей), все экраны развёрнуты без ошибок, «Неделя» 9–15 марта — «Учёт в 7 днях из 7»; норма формой на истории — «выполнена в 0 из 7 недель · 1 неделя не ясна»
- Порча в `git worktree`, по одной на прогон: без задетых норм — код 1, 2 проверки; ✓ всегда — код 1, 4; норма красным — код 1, 1. Рабочая копия убрана, переход снят
- Поиск `indexedDB` вне `core/db.ts` и тестов — пусто
- Деплой `dd9dfbd` — `success`

*Проверено на устройстве* — ничего

*Не проверено*

- «Неделя» на узком экране глазами — только прогоном; таблица приёмов по дням в четыре столбца и форма нормы с 25 галочками
- Синхронизация норм между устройствами — хранилище `norms` было в раскладке с Этапа 0, прогон синхронизации его не трогает

**Не описано в документах — на обсуждение**

- **Акцент интерфейса тёплый при зелёной иконке** — решением нигде не записано
- **История по всем неделям** со временем размоет «было — стало»: через год «выполнена в 30 из 52» не покажет перемену последних недель. Окно или отметки последних недель — когда история накопится
- **Ручного порядка норм нет** — по порядку заведения (Р-25, «Цена»)
- **«Как вчера» не называет задетые нормы** — строка только у тапа (Р-25); блок «Нормы недели» при этом верен
- Прежние — `share_target`; незаполненный день — к Этапу 4; вынос ядра (Р-22)

**Следующий шаг**

Человек на телефоне: свои нормы на «Неделе», история февраля–марта, запись сладкого на «Сегодня», вёрстка. Подтверждено — Этап 3 закрывается; `/clear` и Этап 4 «Повтор приёма» новой сессией.

---

## 17.09.2026 — Этапы 1 и 2 закрыты проверкой человека

**Решения**

- **Р-22 — вынос общего ядра — когда «Трапеза» доделана, до планирования приложения финансов.** Уточняет срок Р-06 «после Этапа 2». Выбор человека
- Незаполненный день для напоминания — решает человек в Этапе 4; открытый вопрос оставлен
- Список блюд в выборе приёма и поиске — структурой по группам и категориям, ручной порядок — пунктом 5 Этапа 4, с развилкой до кода

**Проверено на устройстве** — человеком: история и стартовые блюда загружены на компьютере, синхронизация работает, на компьютере и на телефоне всё отобразилось. Этапы 1 и 2 закрыты, из Плана убраны

*Не подтверждено словами — хвостами в Плане*

- Режим полёта на телефоне
- Обычный день на телефоне меньше чем за минуту
- Прогон `--data` на копии настоящих данных — копии в `seed/` нет

**Находки человека**

- Блюда на экранах — сплошной кучей; хочется структуры по категориям в выборе приёма, поиске и других местах. В Этап 4, п. 5

**Не описано в документах — на обсуждение**

- Тон отклика норм — решается до кода Этапа 3, первым делом следующей сессии
- `share_target` — открыт

**Следующий шаг**

`/clear` — и Этап 3 «Нормы и неделя» новой сессией по передаче в `06-Процесс`.

---

## 17.09.2026 — Этап 2: код синхронизации на сайте; история записана, ждёт загрузки

**Решения** — записаны в момент выбора, `7b0a56d`

- **Р-20 — одно блюдо в одном приёме с двух устройств до обмена — две записи,** без автослияния; лишняя удаляется тапом. Рекомендация, принятая человеком
- **Р-21 — записи еды импортируются на одном устройстве,** второе получает их синхронизацией; строка в «Импорте записей». История — на компьютере: выбор человека
- Таблица столбцов истории и стартовые блюда — «да» человека. Общие блюда истории наверху списков — принято; ручной порядок блюд и категорий — в «Отложено»
- Открытый вопрос о выносе ядра дополнен: решается до кода приложения финансов — «да» человека. «Незаполненный день» — пояснён

**Сделано** — четыре коммита, запушено, деплой `ff0a750` — `success`

- `7b0a56d` — Р-20, Р-21, открытые вопросы, План Этапа 2
- `ebaa343` — `ui/SyncSettings.tsx`, `ui/syncDot.ts` с тестом — `git archive d86f0aa`, blob-хеши трёх сверены
- `ff0a750` — `startAutoSync` в `app.tsx`; «Синхронизация» первым разделом «Настроек»; точка на шестерёнке «Сегодня»; строка Р-21; прогон на подставном GitHub — блок из прогона «Делу Время» `d86f0aa`, сценарий «Трапезы»; «Найдено N из M блюд» — склонение после «из»
- Вне репозитория: `seed/trapeza-import-2026-02-03.json` записан скриптом после «да» по таблице
- Этот коммит — Журнал и План

**Находки по ходу**

- В выводе прогона — «Найдено 1 из 3 блюда»: проверка искала подстроку «блюд» и пропустила ошибку склонения. Склонение после «из» — своими формами, проверка — регулярным выражением
- Порча трёх поломок сразу маскирует автозапуск: «уехало обратно» не сходится и без него, из-за сломанного слияния. Автозапуск проверен отдельным прогоном
- Вырезка блока из чужого прогона по номерам строк оборвала функцию на закрывающей скобке — `node --check` поймал до прогона
- Heredoc в Bash на длинном сценарии с шаблонными строками JS снова споткнулся — сценарий положен файлом через инструмент записи

**Состояние: код Этапа 2 на сайте; Этапы 1 и 2 не закрыты — ждут человека**

*Проверено машинно*

- `npm test` — код 0, 363 теста в 24 файлах; тест истории выполнен на записанном файле — 3 из 3
- `npm run build` — код 0; `npm run smoke` — код 0, 53 проверки: синхронизация выключена по умолчанию; «Проверить доступ» — приватный, запись разрешена, срок токена; пустой репозиторий заведён, один коммит; раскладка `intake/2026-02.json` и `intake/ГГГГ-ММ.json`, годовых нет; README «Трапезы»; тихий повтор без коммита; второй «Борщ» с другого устройства слит, запись у оставшегося, уехало обратно; ужин другого устройства в своём дне; отказ токена — причина и красная точка; без связи — очередь и серая точка, связь вернулась — доехало
- Порча в `git worktree`: без слияния и с нарезкой годом — код 1, семь проверок; только без автозапуска — код 1, две проверки. Рабочая копия убрана
- Деплой `ff0a750` — `success`

*Проверено на устройстве* — ничего: живого репозитория в работе ещё не было

*Не проверено*

- Настоящий GitHub: Contents API на пустом `TrapezaData`, заголовок срока токена, CORS — только подставным
- `npm run smoke -- --data` — копии настоящих данных нет: история ещё не загружена

**Не описано в документах — на обсуждение**

- Прежние пункты — тон норм, `share_target`, незаполненный день к Этапу 4 — открыты

**Следующий шаг**

Человек, по порядку: загрузить историю и стартовые блюда на компьютере; копию файлом — в `seed/`, прогон `--data`; обычный день на телефоне — Этап 1 закрыт. Затем токены, синхронизация с компьютера, потом с телефона, режим полёта — Этап 2 закрыт. После — сессия планирования выноса ядра.

---

## 16.09.2026 — Этап 1: код учёта на сайте; история и стартовые блюда ждут «да»

**Решения** — записаны в момент выбора, `c77d31c`

- **Р-17 — окно частоты — месяц до просматриваемого дня;** пустое окно сводится ко всей истории до дня. Рекомендация была 28 дней; месяц — выбор человека. Повод: история — февраль–март, окно от сегодня было бы пустым
- **Р-18 — порции:** тап пишет одну, правка шагом ½ и полем; итог дня — в порциях, граммы без веса порции — одна порция со счётчиком. Закрыт открытый вопрос «шаг дробных порций»
- **Р-19 — перенос таблицы:** категорий 25, а не 26; легенда цветов, `B7E1CD` — лаймовый; синий «доп» — перекус; неясные ячейки — по порядку приёмов, в отчёт; стартовый список собирает Claude по промпту приложения; скрипт — Python без зависимостей в `seed/`

**Сделано** — одиннадцать коммитов, запушено, деплой `success`

- `e4518b4` — `registry.ts` с тестом, `ImportRecords.tsx`, `ui/today.ts` с тестом, `useToday`, `useNow` — `git archive d86f0aa`, blob-хеши всех семи сверены
- `303dc8e` — `food/names.ts`, `catalog.ts`: id из названия («ё», регистр, пробелы), удаление с переносом, `reconcilePlan` — перенос доезжает до блюд, записей, шаблонов и норм
- `e819ceb` — `food/import.ts`: разделы `categories`, `dishes`, `intake`; повтор записи внутри файла — в отчёт
- `c63b8d5` — правки копии: реестр с разделами импорта списком и разбором по порядку; промпт `core/importing.ts` без минут, оценка — только где сказано; вход «Импорт записей» в «Настройках»
- `ba2aef4` — вкладка «Блюда»: по категориям, поиск, форма блюда, категории с группой и порядком, архив, удаление переносом; `watchMerges` в `app.tsx`
- `b2fe295` — «Сегодня»: четыре приёма, у текущего список открыт сам, тап — запись, повторный тап — порция к той же записи; правка записи; день стрелками, календарём и `?day=`
- `a7baed7` — порядок блюд по частоте и «как вчера»
- `2053e08` — итог дня по группам и категориям, ккал с основанием
- `52b0ef0` — тест истории таблицы на `seed/`, без файла пропускается
- `c88a970` — прогон: частота проверяется против алфавита (после порчи)
- Вне репозитория, в `seed/` под `.gitignore`: `xlsx-to-import.py` и `trapeza-import-starter.json`

**Находки по ходу**

- Заголовок столбца V — «Рыбные » с пробелом; скрипт сравнивает без пробелов по краям
- Неясных ячеек пять: компот без заливки с 1 и 2 порциями — четыре, морепродукты коричневым с 1 — одна; разложены по порядку приёмов (Р-19)
- История даёт 425 записей на 433 порции: одинаковые блюда приёма — одной записью с `portions`
- Стартовый список после истории: 52 новых блюда, 26 уже есть (25 категорий и «Компот»), замечаний ноль — разбором самого приложения
- В прогоне шаг, начатый с `[`, склеивался с последней строкой помощников — `act` теперь ставит точку с запятой
- Первая проверка частоты прошла на порче: алфавит совпал с частотой. Сценарий переделан так, чтобы частотный порядок был обратен алфавитному
- Правки файлов Python-скриптом через heredoc теряют обратную косую в `\s`; с кириллицей надёжнее инструмент правки, чем скрипт

**Состояние: код Этапа 1 на сайте; этап не закрыт**

*Проверено машинно*

- `npm test` — код 0, 356 тестов в 22 файлах, тест истории пропущен без файла; с файлом в `seed/` — 3 из 3: без замечаний, 271 и 162 порции, 28 и 19 дней, повтор — ноль
- `npm run build` — код 0; `npm run smoke` — код 0, 39 проверок: импорт текстом со сводкой, «Блюда», новое блюдо формой, слияние одноимённого из копии, запись тапом, порция повторным тапом, правка шагом ½ и временем, перезагрузка, прошлый день по адресу, частота, «как вчера», итог дня с основанием, без сети
- Порча в `git worktree` — тап без записи, порядок без частоты, калории без основания: код 1, семь проверок не сошлось; каждая порча — на своей. Рабочая копия убрана
- Скрипт истории: 271 порция за 28 дней, 162 за 19 — сходится с суммами таблицы
- Поиск `indexedDB` вне `core/db.ts` и тестов — пусто
- Деплой до `52b0ef0` — `success`

*Проверено на устройстве* — ничего: учёт на телефоне не открывали

*Не проверено*

- `npm run smoke -- --data <копия>` — настоящей копии данных ещё нет: на устройстве пусто. Миграций в этапе нет, `V1_STORES` не тронут
- Вёрстка «Сегодня» и «Блюд» на узком экране — только прогоном, глазами не смотрел

**Не описано в документах — на обсуждение**

- **Таблица столбцов истории** — названия категорий и общих блюд придуманы мной по заголовкам («Супы дефолтные» → «Супы обычные», «Сырные продукты» → «Сырное», общее блюдо «Сладкое», «Кофе или чай»). Р-19 требует «да» до записи файла
- **Стартовые блюда с оценками калорий** — мои оценки, справочно (Р-03); смотреть до загрузки
- **Общие блюда истории встанут в списки приёмов наверх** — «Каша», «Суп обычный» чаще всего были в феврале–марте; порядок по всей истории поставит их над «Гречкой» и «Борщом», пока не накопится месяц новых записей. Цена Р-09, но видна она только теперь
- **Импорт истории — на одном устройстве:** у записей ULID, и та же история, загруженная на телефоне и на компьютере до синхронизации, раздвоится. В Плане сказано; решением не записано
- Прежние пункты — тон норм, `share_target`, незаполненный день, вынос общего ядра до приложения финансов — открыты

**Следующий шаг**

Человек смотрит таблицу столбцов и стартовые блюда. После «да» — `python seed/xlsx-to-import.py --write`, тест истории, загрузка на телефоне, обычный день на телефоне — и Этап 1 закрывается.

## 16.09.2026 — Этап 0: каркас на сайте и на телефоне — закрыт

**Решения**

- **Р-16 — ядро «Делу Время» берётся по этапам,** тем, что этап подключает, плюс `core/` целиком; таблица «что — в каком этапе» — в записи. Ярлык «Записать» объявлен с Этапа 0 адресом `?go=write`. Рекомендация, принятая человеком. План, п. 1–3 Этапа 0, и Архитектура ссылаются на неё

**Сделано** — пять коммитов, запушено

- `978bcb5` — Р-16, План, Архитектура
- `4323c8e` — копия `git archive d86f0aa`, 56 файлов строки «0» таблицы Р-16; blob-хеши всех 56 сверены с `d86f0aa`
- `ce1afc1` — правки по делу: модель дословно из Архитектуры, `SCHEMA_VERSION = 1`, пустые `migrations`, `RecordKind = 'intake'`; база `trapeza`, `V1_STORES` из пяти, индекс `intake.date`; `PLACES` — справочники одним файлом, `intake/ГГГГ-ММ.json`, испорченная дата — `undated`; README данных, сообщения коммитов, `trapeza-import`; `base` `/Trapeza/`, манифест без `share_target` с одним ярлыком; `sw.ts` без напоминаний; своё пустое «Сегодня»; «Настройки» — копия файлом и «О приложении»; первая запись «Что нового». Тесты `db`, `layout`, `sync`, `feed`, `firstRun` — на хранилищах «Трапезы»
- `440adef` — своя иконка: тарелка и ложка, зелёный акцент (у «Дневников» синий, у «Делу Время» тёплый)
- `d97d9d5` — прогон Этапа 0: 23 проверки. Подставной GitHub и сценарии «Делу Время» сняты до своих этапов; `--data` оставлен с маршрутами `/` и `/settings`

**Находки по ходу**

- Общие файлы «Делу Время» сцеплены с их модулями плотнее, чем говорил План: `registry`, `notify`, `Help`, `app`, `Settings`, через `notify` — и `sw.ts`. Отсюда Р-16
- `firstRun.isEmptyBase` у них не считает справочники: категории заводятся сами. Здесь стартовых нет (Р-15), и пустой считается база без справочников тоже — импорт во вкладке теряется так же, как записи. Правка по делу, решением не записана: следствие Р-15
- Промпт импорта в `core/importing.ts` — правила 3–4 про минуты и занятия. В Этапе 0 не вызывается; переписывается в Этапе 1 — строка в Плане
- `core/feed.test.ts`: вид записи один, и порядок видов в ленте проверяется на двух выдуманных — `SECOND`, `THIRD`
- Pages в репозитории уже были включены с источником, который принял `deploy-pages`: первый деплой прошёл без правок настроек
- Heredoc в Bash спотыкается о кириллицу с кавычками в длинных скриптах — правки файлов шли скриптами Python из папки сессии

**Состояние: Этап 0 закрыт — каркас на сайте и на телефоне**

*Проверено машинно*

- `npm test` — код 0, 273 теста в 14 файлах; `npm run build` — код 0
- `npm run smoke` — код 0, 23 проверки: манифест и иконки под `/Trapeza/`, ярлык «Записать» один, `share_target` нет, база `trapeza`, настройки шестерёнкой, копия `trapeza-ГГГГ-ММ-ДД.json` с пятью хранилищами без токена, восстановление, работник активен, без сети — из кеша по ярлыку, данные на месте
- Порча в `git worktree`: `(undefined).length` в заголовке «Сегодня» — прогон код 1, 13 проверок не сошлось, 4 ошибки консоли. Рабочая копия убрана
- Поиск `indexedDB` вне `core/db.ts` и тестов — пусто
- Манифест сверен по `dist/` и по сайту; деплой `d97d9d5` — `success`; `goigolden88.github.io/Trapeza/`, `manifest.webmanifest`, `sw.js`, иконка — 200

*Проверено на устройстве* — человеком, на телефоне: ставится иконкой, открывается без сети, ярлык «Записать», новая сборка доехала сама — дата сборки в «Настройках» сменилась без переустановки. Этап 0 закрыт, из Плана убран

*Не проверено*

- «Сообщить об ошибке» на сайте — issues в `Trapeza` включены (`has_issues`), ссылку не открывал

**Не описано в документах — на обсуждение**

- **Когда решать вынос общего ядра.** Р-06: после Этапа 2. Приложение финансов будет четвёртым — если его код начнётся раньше решения, оно молча станет четвёртой копией. Предложение: в открытом вопросе `05` дописать «решается до кода приложения финансов; первое известное расширение ядра — импорт не-JSON, CSV выписки». Не записано — ждёт «да» человека
- Прежние пункты 15.09 — тон норм, `share_target`, незаполненный день, шаг дробных порций — открыты

**Следующий шаг**

`/clear` — и Этап 1 «Учёт» новой сессией по передаче в `06`.

## 15.09.2026 — Планирование: пятнадцать решений, документы, кода нет

Сессия планирования в Claude Code из папки `Еда`, в режиме планирования. Репозитория ещё не было — он заводится после выбора имени, — поэтому решения записывались готовыми записями в файл плана в момент выбора и перенесены в `05` дословно.

**Решения — все записаны в момент выбора**

Каждая развилка — с рекомендацией и умолчанием; человек выбирал сам.

- **Р-01 — вопрос приложения — состав и режим недели нормами; калории справочно.** Цели по калориям и весу нет
- **Р-02 — единица записи — блюдо из своего списка;** категория и калорийность — его свойства
- **Р-03 — калорийность и стартовые блюда — свой импорт по промпту для ИИ;** внешних баз нет, Open Food Facts отложен
- **Р-04 — платформа — PWA семьи;** обёртка в APK — запасной путь с проверяемым поводом
- **Р-05 — здесь только еда и напитки;** здоровье и спорт — «Дневники» и Health Connect телефона
- **Р-06 — ядро копируется из «Делу Время» с `d86f0aa`;** вынос общего — после Этапа 2, по трём диффам; договор семьи — в Архитектуре
- **Р-07 — рецепт — необязательная часть блюда,** поздним этапом
- **Р-08 — повтор приёма — частота, «как вчера» и свои шаблоны** из записанного
- **Р-09 — февраль–март таблицы — записями;** разовый скрипт переводит xlsx в свой формат
- **Р-10 — название — «Трапеза», `Trapeza`.** Выбор человека; рекомендация была «Щи да каша» — записана отвергнутой с доводами
- **Р-11 — запись — одно блюдо в одном приёме дня;** приём по часам, перекус явно; один кодовый модуль `food`
- **Р-12 — id категорий и блюд — из названия;** одноимённые сливаются, как категории «Делу Время»
- **Р-13 — нормы недели — своя запись `Norm` над набором категорий.** Отступление от «Делу Время»: `since` необязательно
- **Р-14 — время приёма — необязательное поле `at`.** Вариант человека: «опционально, необязательный признак у записи»; автоподстановка — позже и только для текущего приёма
- **Р-15 — категории на старте — 26 столбцов таблицы;** группы — укрупнения листа итогов

Порядок этапов — «синхронизация до новых сущностей, нормы раньше шаблонов» — выбран человеком; записан в `03`, «Логика порядка», не отдельным решением.

**Сделано**

- Разобрана таблица `Еда\Исходники\Учет Еды 2026.xlsx` — значения, формулы и цвета заливки, по XML файла; раздел «Проблема» в `01`
- Прочитаны `CLAUDE.md` и `docs/` «Делу Время», `01` и модель «Дневников», skills `doc-driven-project` и `local-first-pwa`
- Сверены ядра «Делу Время» и «Дневников» по хешам (Р-06)
- `CLAUDE.md`, `docs/01–06`, `.gitignore`, `.gitattributes`; репозиторий `Еда\Trapeza`, коммит документов

**Находки по ходу**

- Таблица умерла не «через месяц»: январь с 13-го перенесён из Obsidian без цветов, февраль — целиком, март — по 19-е. Сводка марта не собрана
- Компот — 74 из 271 порции февраля, 27%; каша — 23 дня из 28, суп — 26 из 28. Самое частое — постоянное
- Время четырёх приёмов вписывалось каждый день и не попало ни в одну сводку
- Сумма марта считается по строкам 2–29 — формулу февраля не растянули; в столбце времени 20–27 марта — хвосты копирования февраля
- «Дневники» с `a913dcb` не менялись — ноль коммитов; ядро «Делу Время» с тех пор прошло 17 коммитов, из 16 общих файлов `src/core/` побайтно совпадает один `id.ts`
- **В «Дневниках» ошибка `syncNow` на месте** — `src/core/sync.ts:462-465`: ветка «не настроена» выходит, не отпустив `running`, и синхронизация, включённая после старта, не идёт до перезапуска. В «Делу Время» исправлена их `b49f37f`. «Дневники» здесь только читаются — сказано человеку, чинится отдельной сессией там
- Скриншотов в `Исходниках` нет — только xlsx; разбор по нему
- Рабочая папка на диске — `Еда`, а не «Учет еды»

**Ошибки по ходу, мои**

- **«Больше 80% — домашняя еда».** Принял «Домашняя еда 82,7%» листа итогов марта за дом против кафе; на деле это еда против напитков и алкоголя (в феврале та же доля подписана «Еда»). Опирался на это в развилках 1 и 2. Поймал, когда считал состав для концепта, — до переноса в документы; сказал человеку, Р-02 и Р-03 записаны с верными цифрами: кафе и фастфуд — 4 порции из 271, упакованное — около 10%. Выводы не изменились. Ловится теперь: цифра с листа сводки — только вместе с формулой, из которой она посчитана

**Состояние: планирование закрыто, кода нет**

*Проверено машинно*

- Поиском по `docs/` и `CLAUDE.md`: заготовочных `<…>` и `ДД.ММ.ГГГГ` нет; Р-01…Р-15 подряд, без пропусков
- `git status` после коммита — чистый; в коммите девять файлов — `.gitattributes`, `.gitignore`, `CLAUDE.md`, шесть docs; личных данных нет; запушено в `goigolden88/Trapeza`

*Проверено на устройстве* — нечего: кода нет

*Не проверено* — всё о коде; с Этапа 0

**Не описано в документах — на обсуждение**

- Тон отклика норм — красить ли превышение на «Сегодня» (открытый вопрос в `05`, к Этапу 3)
- `share_target` не объявлен: принимать приложению нечего. Понадобится — например, фото еды — отдельным решением
- Что считать «незаполненным днём» для напоминания — к Этапу 4
- Шаг ввода дробных порций («полпорции», «нарезной долями») — к Этапу 1

**Следующий шаг**

Человек заводит на GitHub пустой публичный `goigolden88/Trapeza`; пуш документов. `/clear` — и Этап 0 новой сессией из корня `Еда\Trapeza` по промпту из `06-Процесс`, «Передача в следующую сессию».

import { Link } from 'react-router-dom'
import { InstallNote } from '../ui/Install.tsx'
import { quoted } from '../ui/screenNames.ts'
import { useScreenNames } from '../ui/useScreenNames.ts'

/**
 * Приветствие на главном экране при первом запуске: что это, что уже
 * работает, как поставить иконкой.
 *
 * Показывается, пока в базе нет ни одной записи и его не закрыли. Первая
 * запись убирает его сама — дело сделано; «Понятно» — насовсем.
 *
 * Числа — из констант, цифрой в этом файле не пишется ничего: это проверяет
 * тест (правило из CLAUDE.md). Названия экранов — из настроек (Р-26).
 */
export function Welcome({ onDone }: { onDone: () => void }) {
  const names = useScreenNames()

  return (
    <section className="panel block">
      <h2>С чего начать</h2>
      <p>
        Делу Время — план дня, заметки и учёт времени. Всё хранится на этом устройстве и работает
        без сети; без настройки наружу ничего не уходит.
      </p>
      <ul>
        <li>
          <b>
            <Link to="/inbox">{names.inbox}</Link>
          </b>{' '}
          — мысль, дело или замысел одной строкой. На Android у установленного приложения — из любого
          приложения через «Поделиться» → «Делу Время» или долгим тапом по иконке → «Записать».
        </li>
        <li>
          <b>Копия данных</b> — <Link to="/settings">«Настройки»</Link> → «Экспорт и импорт»: файл,
          из которого всё восстанавливается, и способ перенести записи на другое устройство.
        </li>
        <li>
          <b>
            <Link to="/time">{names.time}</Link>
          </b>{' '}
          — кнопки категорий здесь и на экране {quoted(names.time)}: тап ставит блок на сегодня, итог
          дня виден сразу. Там же таймер, ввод задним числом, категории и кнопки.
        </li>
        <li>
          <b>{names.today}</b> — план дня: дело из неразобранного одним тапом или сразу полем, главное дело
          дня, влезает ли намеченное в остаток дня. Работу и дорогу стоит вписать пунктами с оценкой — иначе
          сравнивать не с чем.
        </li>
        <li>
          <b>Обзор недели</b> — раз в неделю, ссылкой внизу экрана {quoted(names.today)}; там же итоги
          месяца и года, лента всех записей. Как всё устроено — <Link to="/help">справка</Link>, «?» рядом
          с шестерёнкой.
        </li>
      </ul>

      <h3 className="unit__name">Установка</h3>
      <InstallNote empty />

      <div className="row row--wrap">
        <button type="button" className="btn btn--primary" onClick={onDone}>
          Понятно
        </button>
      </div>
    </section>
  )
}

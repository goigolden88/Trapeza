import { Link } from 'react-router-dom'
import { InstallNote } from '../shared/ui/Install.tsx'

/**
 * Приветствие на «Сегодня» при первом запуске: что это, с чего начать, как
 * поставить иконкой.
 *
 * Показывается, пока в базе нет ни одной записи — ни блюд, ни записей — и
 * его не закрыли. Первое блюдо или загруженный список убирает его сам —
 * дело сделано; «Понятно» — насовсем.
 *
 * Числа — из констант, цифрой в этом файле не пишется ничего: это проверяет
 * тест `help.test.ts` (CLAUDE.md). Взято из «Делу Время» с d86f0aa; названия
 * экранов у «Трапезы» постоянные (Р-16), текст — свой.
 */
export function Welcome({ onDone }: { onDone: () => void }) {
  return (
    <section className="panel block">
      <h2>С чего начать</h2>
      <p>
        Трапеза — учёт еды по блюдам: что съедено, в каком приёме, и совпала ли неделя с тем, что себе
        наметил. Всё хранится на этом устройстве и работает без сети; без настройки наружу ничего не уходит.
      </p>
      <ul>
        <li>
          <b>Блюда</b> — сначала свой список. Руками — <Link to="/dishes">«Блюда»</Link> → «Новое блюдо»;
          списком из заметок или таблицы — <Link to="/settings">«Настройки»</Link> → «Экспорт и импорт» →
          «Импорт записей», там же промпт для ИИ.
        </li>
        <li>
          <b>Запись</b> — тап по блюду в приёме на «Сегодня». Приём ставится по часам, порция — одна; граммы,
          время и заметка — по желанию. Привычное повторяется «как вчера», шаблонами и «Как обычно?».
        </li>
        <li>
          <b>Неделя</b> — нормы вроде «сладкое — не больше стольких-то дней»: заводятся на{' '}
          <Link to="/week">«Неделе»</Link>, отклик виден сразу после записи.
        </li>
        <li>
          <b>Копия данных</b> — «Настройки» → «Экспорт и импорт»: файл, из которого всё восстанавливается,
          и способ перенести записи на другое устройство.
        </li>
        <li>
          Как всё устроено — <Link to="/help">справка</Link>, «?» в шапке «Сегодня»; ⌕ рядом — лента всех
          дней с поиском.
        </li>
      </ul>

      <h3 className="unit__name">Установка</h3>
      <InstallNote empty />

      <div className="row row--wrap">
        <button type="button" className="btn btn--primary" onClick={onDone}>
          Понятно
        </button>
      </div>
    </section>
  )
}

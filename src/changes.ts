/**
 * Что нового: список изменений, которые видно человеку.
 *
 * Пишется руками, в том же коммите, что и само изменение, — правило
 * в `CLAUDE.md`. Сборка без видимых изменений строки не получает, и после
 * неё ничего не показывается. Окно «Что нового» после обновления — Этап 5;
 * список ведётся с первого дня и виден в «Настройках» → «О приложении».
 *
 * Записи «Делу Время», у которых механика взята, сюда не перенесены: список —
 * история этого приложения (Р-16).
 *
 * Числа в строках — история: правда в день записи и остаётся ею. Правило
 * «текст из констант» — для текстов, которые описывают, как приложение
 * работает сейчас.
 *
 * Механика взята из «Делу Время», у них — из «Дневников».
 */

import type { DateStr } from './core/dates.ts'

export type Change = {
  /** Растёт на единицу с каждой записью. По нему устройство помнит прочитанное. */
  id: number
  date: DateStr
  lines: readonly string[]
}

/** От старых к новым. Новая запись — в конец. */
export const CHANGES: readonly Change[] = [
  {
    id: 1,
    date: '2026-09-16',
    lines: [
      'Первая сборка «Трапезы»: ставится иконкой на телефон, открывается без сети, обновляется сама. Дата сборки — «Настройки» → «О приложении».',
      'Копия всех данных — «Настройки» → «Экспорт и импорт»: сохранить в файл, поделиться, восстановить.',
      'Долгий тап по иконке — ярлык «Записать». Пока он открывает «Сегодня»; запись приёмов появится следующими обновлениями.',
    ],
  },
  {
    id: 2,
    date: '2026-09-16',
    lines: [
      'Импорт блюд, категорий и записей еды — «Настройки» → «Экспорт и импорт» → «Импорт записей». Промпт для ИИ там же; до записи видно, что добавится, а что уже есть.',
    ],
  },
  {
    id: 3,
    date: '2026-09-16',
    lines: [
      'Вкладка «Блюда»: блюда по категориям, поиск, порция и калорийность, категории с группами и порядком, архив.',
      'Блюдо с записями удаляется только переносом записей в другое — так же сливаются два одинаковых. Одноимённые блюда из копии сливаются сами.',
    ],
  },
  {
    id: 4,
    date: '2026-09-16',
    lines: [
      '«Сегодня»: завтрак, обед, ужин и перекус. У текущего приёма список блюд открыт сам — запись одним тапом; повторный тап прибавляет порцию.',
      'Тап по записи — правка: приём, порции шагом в половину, граммы, время и заметка по желанию, удаление.',
      'Любой прошлый день — стрелками или календарём, записи задним числом так же.',
    ],
  },
  {
    id: 5,
    date: '2026-09-16',
    lines: [
      'Блюда в приёме — по частоте: наверху то, что чаще бывало в этом приёме за последний месяц.',
      '«Как вчера» у приёма — прошлый такой же приём одним тапом, без повторов того, что уже записано.',
    ],
  },
  {
    id: 6,
    date: '2026-09-16',
    lines: [
      'Итог дня под приёмами: порции по группам и категориям, калории — с тем, по скольким записям они посчитаны. Блюда без категории названы отдельно.',
    ],
  },
  {
    id: 7,
    date: '2026-09-17',
    lines: [
      'Синхронизация через свой приватный репозиторий на GitHub — «Настройки» → «Синхронизация»: репозиторий, токен, «Проверить доступ», срок токена. Запись уезжает сама; без сети ждёт в очереди.',
      'Точка на шестерёнке «Сегодня»: серая — есть неотправленное, красная — обмен не прошёл.',
      'В «Импорте записей» — правило: записи еды загружать на одном устройстве, остальные получат их синхронизацией.',
    ],
  },
  {
    id: 8,
    date: '2026-09-17',
    lines: [
      'Вкладка «Неделя»: любая неделя стрелками или календарём; в скольких днях был учёт, порции по дням — тап открывает день, состав по группам с числом дней, приёмы по дням, калории в среднем с основанием.',
      'Нормы — «Неделя» → «Нормы»: набор категорий и сколько дней в неделю — не меньше или не больше. У нормы — итог недели и в скольких неделях она выполнена. В счёт идут недели, чей исход ясен: день без записей мог быть любым. Без цвета — числом.',
    ],
  },
  {
    id: 9,
    date: '2026-09-17',
    lines: [
      '«Сегодня»: блок «Нормы недели» — как идут нормы на неделе этого дня. После записи строка под приёмами называет нормы, которые она задела, — и говорит, если день уже был в счёте.',
    ],
  },
  {
    id: 10,
    date: '2026-09-17',
    lines: [
      'Выбор блюда в приёме — не россыпью: наверху «Частые» в этом приёме, ниже все блюда по категориям, свёрнутыми. Что развёрнуто, телефон помнит. Поиск показывает найденное под названиями категорий.',
    ],
  },
  {
    id: 11,
    date: '2026-09-17',
    lines: [
      'Шаблоны: «Сохранить как шаблон» у записанного приёма и «Сохранить день как шаблон» под приёмами. Шаблон ставит свои блюда одним тапом и не дублирует уже записанное. Шаблон дня на сегодня пишет только начавшиеся приёмы — несъеденный ужин утром не запишется.',
      'Шаблоны переименовываются, удаляются и переставляются на «Блюдах», в блоке «Шаблоны». Состав меняется пересохранением под тем же названием.',
    ],
  },
  {
    id: 12,
    date: '2026-09-17',
    lines: [
      '«Как обычно?» наверху «Сегодня»: вчерашние завтрак, обед и ужин без записей и сегодняшние до текущего приёма — одним тапом. Предлагается шаблон приёма, иначе приём из шаблона дня, иначе блюда, которые были хотя бы в половине последних таких приёмов, — на кнопке сказано, по скольким. «Не было» — приём больше не спрашивается.',
    ],
  },
  {
    id: 13,
    date: '2026-09-17',
    lines: [
      '«Неделя»: нормы переставляются стрелками ↑ ↓ — главная встаёт первой. Порядок общий для всех устройств и тот же в «Нормах недели» на «Сегодня».',
    ],
  },
]

/**
 * Что показать после обновления и что отметить прочитанным.
 *
 * `seen` — последний прочитанный `id`, null — ключа на устройстве нет.
 * Ключа нет у двух разных установок, и различает их база:
 *
 * - пустая — свежая установка: ей всё новое, и список изменений ей ни о чём
 *   не говорит. Показываем ничего, отмечаем прочитанным всё
 * - с записями — копия, обновившаяся с версии, где этого окна не было:
 *   показываем последнюю запись, ради неё окно и появилось
 */
export function unseenChanges(
  changes: readonly Change[],
  seen: number | null,
  empty: boolean,
): { show: Change[]; markSeen: number | null } {
  const latest = changes.at(-1)?.id ?? null
  if (seen === null) {
    if (empty) return { show: [], markSeen: latest }
    const last = changes.at(-1)
    return { show: last ? [last] : [], markSeen: null }
  }
  return { show: changes.filter((change) => change.id > seen), markSeen: null }
}

/** Последний `id` — его пишет «Понятно». */
export function latestChange(changes: readonly Change[]): number {
  return changes.at(-1)?.id ?? 0
}

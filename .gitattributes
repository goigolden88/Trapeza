# Переводы строк нормализуются в LF в репозитории.
# Без этого Windows и CI дают разные диффы на одних и тех же файлах.
* text=auto eol=lf

# Бинарники не трогать.
*.png binary
*.ico binary
*.woff2 binary
